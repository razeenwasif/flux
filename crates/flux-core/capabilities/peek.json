{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "flux-peek",
  "description": "Peek / glance windows host REMOTE page content and, like tab webviews, get ONLY the inlined `fluxtab` bridge — here just enough to promote the peek to a real tab or close it (Rust guards those to peek-* windows).",
  "webviews": ["peek-*"],
  "remote": { "urls": ["http://*:*", "https://*:*"] },
  "permissions": ["fluxtab:default"]
}
