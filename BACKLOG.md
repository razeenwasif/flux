# Flux Backlog

Single source of truth for unimplemented work. Numbers are stable — code
comments reference them as `BACKLOG #n`. When an item ships: move its entry to
`CHANGELOG.md` (Unreleased → Added) and delete it here, same commit
(docs-before-commit policy).

Priorities: **P0** = blocks the core demo loop · **P1** = v0.1 release · **P2** = post-release.

## Epic: Core / IPC

| # | P | Item |
|---|---|---|
| 132 | ◐ | **Typed errors (`FluxError`, thiserror) across flux-core** — foundation done: `error.rs` (`Io`/`Json`/`Http`/`NotFound`/`Locked`/`Invalid`/`Other`; `From<FluxError> for String` keeps the IPC contract; serializes for future structured IPC), exemplar migrations `currency.rs` + `pdf.rs`. _Remaining:_ migrate the other ~55 modules opportunistically (when touching a module, its `.map_err(e.to_string())` chains become `?` on `FluxResult`); then consider returning `FluxError` over IPC directly so the frontend can match on error kinds. |
| 125 | ◐ | **Floating music bubble (AudioPulse/Spotify)** — right-edge orb → hover-expand mini-player (play/pause/skip/vol/shuffle/repeat + playlist menu + now-playing), driving the same Spotify Connect playback AudioPulse uses via Flux's spotify backend (`spotify_state`/`spotify_playlists`/`spotify_play_context`). Purple/pink/indigo, breathing orb. **Player core done.** **Real beat-synced visualiser done** — `tools/audioviz` Go helper (PulseAudio monitor → energy/bands → SSE) relayed by `audioviz_stream`; the orb pulses/glows to the actual audio. **Draggable** (done): grab the orb to park it anywhere; position persisted and clamped to the viewport, so a resize can't strand it off-screen. It was pinned mid-right on top of the connections rail, hiding the middle of the list. _Remaining:_ webview-overlap dim-on-expand polish. |
| 121 | ✓ | **Agent-aware terminal — explain / fix** (done) — non-zero exit shows a ⚠ bar with ✦ Explain (Gemma diagnoses the failure in an overlay) and ⚙ Fix (`agent_shell_plan` proposes a corrected command, typed at the prompt for review + Enter). Uses the OSC 133 marks + last-command block; fully local. |
| 122 | ✓ | **Semantic shell-history palette** (done) — `shellhist.rs` reads `~/.bash_history` + `~/.zsh_history` via the terminal's shell (WSL-aware), embeds each unique command with `flux-embed`, and ranks by embedding cosine + keyword/token boosts. Ctrl+Shift+R / palette opens a search overlay; selecting a hit drops it at the active terminal's prompt (or clipboard). `shell_history_search` / `shell_history_reindex`. |
| 123 | ✓ | **Ambient "connects to your knowledge" rail** (done) — as you read a page, embed its text and passively surface your own related Onyx notes / Scroll papers / Council debates in a slim rail. The second brain compounding without being queried. Reuses KB + embeddings. Scoped to **your own writing** (`kb::OWN_SOURCES` — onyx/scroll/council/scribe): visited pages were dropped once the Trail got its own sidebar graph, since listing them in both places showed the same browsing history twice. |
| 124 | ◐ | **Contradiction / novelty check on save** (Onyx saves done — kb_check posts ⚠/↔/➕/✦ verdict + related; _remaining:_ Scroll clips) — when clipping to Scroll or saving to Onyx, the agent checks the new item against the KB and flags "contradicts your note X" or "genuinely new vs your corpus". Research-integrity co-scientist. Reuses `kb_query` + agent. |
| 125 | ✓ | **Weekly local research digest** (done) — the Notebook's "This week" panel: Gemma reviews docs added to the KB in the last 7 days (Onyx + Scroll + Council) and writes Threads / Connections / Open-questions, cached per ISO week, fully local. Needed a real `indexed_at` clock on `KbDoc` (the connector `mtime` is a change-key) + `kb_recent` command + `kbRecent` IPC. |
| 126 | ✓ | **Semantic find — in-page + across tabs** (done) — `semfind.rs` chunks the per-tab captured DOM text into verbatim passages, embeds them (`crate::embedding`, model→hash) + keyword boosts, and ranks vs the query. Overlay (`SemanticFind.tsx`) with This-page / All-tabs scope, opened from the find-bar ✦ or palette; selecting a hit focuses the tab and `window.find`s the passage to scroll+highlight. `semantic_find` command. |
| 127 | P2 | **Select → grounded explain / steelman** — highlight a claim on any page → Gemma explains it cited from your KB, or runs a local steelman+critique (Council-in-the-browser). Co-scientist on every page. |
| 128 | ✓ | **"Watch this page" — local semantic change monitor** (done) — `watch.rs`: 👁 pins a page; a reminders-style background scheduler re-fetches on an interval, strips HTML→text, and diffs by embedding (passages added/removed, not a char diff). OS notification + `flux://watch-changed` on change; `WatchPanel.tsx` lists watches with +/− passages. `watch_add/list/remove/check_now/mark_seen/is_watched`. No cloud — only fetches the watched URL. |
| 129 | ✓ | **Tracker graph (privacy viz)** (done) — `trackers.rs`: the netfilter interceptor records every first-party→third-party request (+ blocked verdict) into a live in-memory graph; `tracker_graph`/`tracker_clear`. `TrackerGraph.tsx` renders it with the Omni-style force-canvas (sites violet, 3rd-parties teal, blocked trackers red; hubs sized by degree). Palette → "Tracker graph". |
| 130 | ✓ | **Calculator / converter / maps home widgets** (done) — start-page widgets, toggle/reorder like the rest. **Calculator** (`Calculator.tsx`): compact keypad → ⤢ scientific (trig DEG/RAD, ln/log, √, powers, factorial, π/e, parens, ANS) via a safe recursive-descent evaluator (no `eval`). **Unit converter** (`Converter.tsx`): length/mass/temp/volume/area/speed/time/data + live **currency** (ECB rates via `currency_rates` backend; frankfurter.app, no key). **Maps**: Australia OSM cover opens the existing map panel (toolbar 🗺 retained). |
| 119 | ✓ | **Omni dashboard graph view** (done) — Obsidian-style force-directed semantic map: top-N docs by PageRank as nodes, cosine-neighbour edges, on a custom canvas sim (drag/zoom/pan/click-to-open). Backed by a new `/graph` endpoint added to the omni repo (top-N + pairwise cosine), proxied via `omni_graph`. Semantic edges chosen over hyperlinks. _Possible follow-up:_ link-edge toggle, colour by cluster, persist layout. |
| 118 | ◐ | **Agent write access to corpora** — Gemma can clip an article to Scroll (`scroll_clip` → /clip form) and save notes to Onyx (`onyx_new_note` → Markdown in the vault, no overwrite), via sidebar intents ("clip this page to scroll", "save that to onyx"). Honours the in-app Scroll-URL / vault-path. _Next:_ richer Onyx writes (append to an existing note, tags/folders from NL), confirmation for destructive edits. |
| 117 | ✓ | **Terminal apps launcher** (done) — a curated/editable bar (sibling to the pages bar) of the user's TUI apps; a chip opens a new Terminal tab auto-running the app's command (cwd-aware). Seeded onyx/scroll/council; ✎ editor (name/icon/cmd/cwd, reorder, ⌕ scan bin dirs); persisted via `tui_apps_*`. Launch is race-free (pending-command consumed by TerminalView after PTY spawn). |
| 136 | ◐ | **Research OS — the Trail / browsing provenance spine** (ADR 0011) — turn browsing itself into a native, cited co-scientist corpus so graph, time-travel, per-page chat, and context search are all *views over one Visit object* (not 30 separate features). **Slice 1 done:** `crate::trace` `TraceStore` — every non-private navigation becomes a **Visit** (url/title/`why`: from-visit + free `Nav` edge + workspace task label), deduped per tab/URL, persisted (`app_data/trace/trace.json`, empty→hydrate→60s-flush, capped/evicted); recorded from `dom_publish` under the existing `if !private` guard (**private windows leave no trace**); `trace_forget` (url/host-boundary/range/all) as the day-one privacy control; `trace_recent`/`trace_visit`/`trace_graph` + specta types. Coexists with History (unchanged). _Next, in order:_ ~~(a) dwell-triggered content snapshot + embed~~ **(done)** — after 8 s dwell the frontend fires `trace_snapshot`; reads cached DOM text, embeds off-thread (`embeddinggemma`/hash), stores in a budgeted `TraceSnapshots` store (`trace/snapshots.json`, 1500 × 20 KiB, oldest-evicted), attaches `snapshot_id`; `trace_forget` cascades, `trace_snapshot_get` reads content; ~~(b) feed the KB as `source:"web"`~~ **(done)** — the Trail's dwell snapshots are a new KB `web` source (chunked+embedded on `kb_reindex`, doc_id = visit id, citation = page URL, self-contained title on the snapshot); shows as "Browsing" in the Notebook; budget-evicted snapshots drop on next reindex. _Follow-up:_ debounced auto-reindex so browsing flows in without hitting ↻ Reindex; ~~(c) Trail view (`flux://trail`)~~ **(done)** — `TrailPage` renders `trace_graph` as a force-directed graph (Omni-style canvas sim); nodes = Visits coloured by task, filled = has snapshot; click → detail panel with provenance + snapshot text (`trace_snapshot_get`); time filter (All/24h/7d/30d) + per-page/window Forget; ⌘K + 🧭 PagesBar chip. _Refinements:_ ~~draggable time-travel scrubber~~ **(done)** — activity-histogram timeline (`trace_histogram`) behind a full-history slider, viewed span as a draggable teal band; ~~render semantic/cites edges distinctly~~ **(done, payoff 1–2)**; ~~(d) per-page chat~~ **(done)** — `TraceChats` (`trace/chats.json`): a persistent Gemma thread per Visit, grounded in the dwell snapshot (told plainly when no capture exists), streamed (`trace_chat`/`trace_chat_send`), 200-msg cap, forgotten with the visit (`trace_forget` cascades to snapshots + chats + KB); chat UI in the Trail detail panel. ~~surface the same thread in the agent sidebar~~ **(done)** — a "💬 Page thread" scope (`trace_tab_thread` re-attaches by visit; replies route through `trace_chat_send`, tail replayed into the feed); ~~then semantic edges → time-travel scrub~~ **(done, payoff part 1)** — capture-time cosine top-3 ≥ 0.55 `Semantic` edges (dashed teal in the Trail, gentle layout pull, both-direction dedup); **KB `web` auto-reindex** (debounced: indexes when snapshots settle for a 60 s tick; embedder-change heals by rebuilding all sources; `FLUX_TRAIL_AUTOINDEX=0` opts out); **time-travel scrub** (slider over `trace_graph(after,before)` windows, up to 8 spans back, ⏪ Reopen brings ≤6 of that moment's pages back as tabs). ~~entity/citation extraction~~ **(done, payoff part 2)** — deterministic scanners (arXiv/DOI/repo/HF+Kaggle datasets, no regex dep, no LLM), URL-primary at nav time + text mentions at dwell; shared entities derive `Implements` (repo→paper) / `Cites` / `Same` edges (magenta long-dash in Trail, entity chips in the detail panel). ~~ambient watchers (local-only)~~ **(done, payoff part 3)** — `trace::ambient` + `trace_ambient`: deterministic error-signature extraction (panics/`error[E…]`/`*Error:`/`fatal:`/OOM-segfault phrases, never prose) matched (case/whitespace-tolerant, different-URL, ≤3 hints) against past dwell snapshots; "⚡ Seen before" block in the Connections rail with a 💬 flag when the past visit has a chat thread. **Payoff layer complete.** _Parked:_ the network-side watcher ("newer arXiv version exists") pending a network-policy decision — would be opt-in, batched, only for visited entities. ~~Draft capture~~ **(done — ADR 0011 COMPLETE)**: opt-in (Settings→Privacy, off by default), `drafts.js` structurally never reads sensitive/login-form fields, Rust re-rejects blocklisted names + Luhn-valid PANs, private tabs dropped, sealed at rest (`trace/sealed.rs` — all trace stores now AES-256-GCM, keychain key + file fallback, transparent migration), capped, forgotten with the visit; 📝 Drafts in the Trail detail panel. _Tuning:_ semantic threshold vs the hash embedder's same-site boilerplate overlap (may over-connect same-domain pages); surface a page's chat thread in the agent sidebar when ON that page. |
| 116 | ◐ | **Knowledge Base / second brain** (ADR 0010) — native local RAG making Gemma a private NotebookLM/co-scientist over the user's own corpora. **Backend done:** `crate::kb` vector store (flux-embed, persisted to `<app_data>/kb/kb-index.json`), **Onyx connector** (`~/OnyxVault/**/*.md`, frontmatter-aware, incremental by mtime), `kb_status`/`kb_reindex`/`kb_query` (cosine top-k) + `kb_answer` (grounded streamed reply with `[n]` citations, like `omni_answer`). Retrieval verified offline via the hash embedder. **Notebook view done** — `flux://notebook` (Ctrl+K → "Open Notebook" / pages-bar chip): ask → streamed grounded answer with clickable `[n]` citation chips, per-source status + Reindex. **Scroll connector done** — reads the read-later library over `localhost:3131/api/articles` (`FLUX_SCROLL_URL` override; skipped gracefully if offline); Scroll citations open the source in a tab. **Sidebar-agent grounding done** — a **✦ My notes** scope in the Liquid AI panel answers from the KB with citations. **Council briefs done** — third source indexing `~/Research/debates/*.md` (question-frontmatter titles). **Specialist voices done** — auto-discovers `<domain>-specialist:*` Ollama models and routes in-domain Notebook/My-notes answers to them (thread-local model override, conservative keyword routing, visible ⚛ badge). KB roadmap complete. |
| 10 | ◐ | CI perf-budget gates per ADR 0001 (done): `scripts/perf-budget.mjs` gates **chrome JS ≤ 50 KB gzip** (eager bundle via the Vite manifest — currently 48.9 KB) + **binary ≤ 25 MB**; `criterion` benches `ipc_roundtrip`/`dom_snapshot` (`benches/ipc.rs`); `.github/workflows/perf.yml` runs all three on PRs (`npm run perf`). _Remaining:_ **idle-RSS / cold-start** gates need a display+webview → self-hosted runner (methodology + Flux-vs-Chrome table in `docs/perf/memory-benchmark.md`); wire those numbers back once recorded. |
| 12 | ◐ | Generate TS types from Rust structs via `specta` (kill the manual `ipc.ts` mirror). _Done:_ `specta::Type` on **51 IPC types** → `bindings.gen.ts` (`bindings.rs::generate_ts`, regen `FLUX_WRITE_BINDINGS=1 cargo test -p flux-core bindings`), drift-gated in CI; `ipc.ts` re-exports them all. Covers `state.rs` (TabKind/TabMeta/TabGroup/TabFolder/ClusterTag/Workspace/Container/WebPanel/AgentStatus) + Bookmark/Feed/FeedItem/PwaApp/HistoryEntry/SavedTab/SavedSession/ProcInfo/SysStats/SpeedResult/ArchiveMeta/CalFeed/CalEvent/Todo, **plus the misc command structs (batch 3):** OmniHit, ReaderBlock, Shields (ShieldsStatus/HotRule/LeanStatus/HttpsStatus/CookieStatus), permissions (PermKind/PermDecision/SitePerm), vault (CredentialMeta/VaultStatus), extensions (Manifest/ContentScript/UiContrib/ToolbarButton/InstalledExt), macros (Step/Macro/MacroStatus), Boost, DownloadItem, files (FileEntry/DirListing/QuickLocation), SyncStatus/SyncReport. **`ArchiveEntry` now has the wire/persist split** — `ArchiveEntryWire` (5 reader fields, specta-generated) is what `archive_get` returns; the persisted struct keeps the embedding/embedder off the wire. **Batch 4 (the struct tail) done:** LaunchIntent, Reminder, MemInfo, HibernateCandidate/EvictionRank, PrefetchHint, `flux-search` (SearchEngine/Resolution), `flux-agent` (AgentAction/ExtractFormat/FileEdit/EditPlan/NextStep), `flux-import` (ChromeProfilePreview/ChromeBookmark — `#[specta(rename)]`d to dodge the Bookmark name clash); specta deps added to those three crates; ipc.ts mirrors swapped to `Gen*` aliases. _Remaining:_ only types with **no Rust struct** (the Omni dashboard's `OmniStats`/`OmniSite`/`OmniAnswer*`, parsed straight from the engine's JSON, plus ad-hoc channel payloads VizFrame/NetProgress/ListMsg). **Command-wrapper codegen (tauri-specta) stays blocked** — it needs the specta v2 / tauri-specta v2 **pre-release** stack (Tauri v2 incompat with the stable specta v1 we use), deferred until runtime-verifiable. |
| 20 | ✅ | **Single-instance forwarding** (done): `tauri-plugin-single-instance` registered first in `run()`; the callback reuses `cli::parse(argv[1..])`, emits `flux://open-url` per URL (and `flux://shortcut new-terminal` for `-t`) into the running window, and unminimizes/shows/focuses `main` — so a second `flux <url>` adds tabs instead of spawning a process. **WSL fix:** running `flux <url>` from a WSL shell was hitting the **Linux** dev build (separate process — the plugin can't bridge Linux→Windows), launching a second "Flux (Ubuntu-…)" window; now the Linux build detects WSL (`cli::in_wsl`) and forwards GUI launches to `flux.exe` (which single-instances into the running Windows Flux), unless `FLUX_LINUX_GUI=1`. _Remaining:_ on-device check that the Windows-side forward (flux.exe → running instance) actually adds the tab. |
| 26 | P2 | Packaging: `.desktop` entry + icon on Linux, PATH shim on Windows/macOS, auto-update channel |

## Epic: Terminal (kitty/ghostty-class)

The bar is a *modern* terminal, not a VT100 museum piece. flux-term has the
grid/damage/renderer spine; these make it competitive with kitty/ghostty:

| # | P | Item |
|---|---|---|
| 3 | P0 | PTY spawn + I/O loop in flux-core (`portable-pty`), env from `terminal_env`; one session per Terminal *tab* and one for the split *pane*; route bytes to `Terminal::advance` |
| 7 | P0 | Glyph atlas: `swash` rasterization, subpixel positioning, theme color resolve, uniform-driven cell metrics (replaces shader constants) |
| 9 | ✅ | **Scrollback ring buffer + reflow-preserving resize** (done, in `flux-term/grid.rs`): rows scrolled off the top go into a bounded `VecDeque` ring (`scrollback`, cap 10k) instead of being discarded; a per-row `wrapped` flag records soft-wraps so `resize` collapses scrollback+screen into logical lines and re-wraps them to the new width (joining/splitting), keeping the cursor at the end of content — no more clear-on-resize. Pure logic, unit-tested (scrollback capture, reflow wider-rejoin / narrower-split, no-loss row shrink). _Note:_ this is the **future WGPU renderer's** grid; the shipping terminal is xterm.js. The renderer (#7 atlas / #13 shaping) stays deferred — visual GPU work, unverifiable headless, and blocked on WGPU-under-webview compositing. |
| 13 | P1 | Font shaping: ligatures + programming-font features via `swash`/`rustybuzz` (kitty parity) |
| 14 | P2 | Kitty graphics protocol — inline images in the terminal (the agent can render charts into the shell) |
| 15 | P1 | Splits and tabs *within* the terminal pane (ghostty-style), keyboard-driven |
| 16 | ✓ | **Shell integration: OSC 133** (done) — bash auto-injects the marks via `bash --rcfile` (re-sources `~/.bashrc`; opt out with `FLUX_NO_SHELL_INTEGRATION=1`); the terminal renders a per-command gutter status bar (running / exit 0 / non-zero), jumps between prompts (`Ctrl+Shift+↑/↓`), and copies the last command's output (`Ctrl+Shift+E`). zsh/pwsh snippets in `docs/shell-integration.md`. WSL forces bash. |
| 17 | ✅ | **OSC 8 hyperlinks + URL detection** (done): clicking a URL in the terminal opens a Flux **browser tab** (closing the terminal↔browser loop) instead of the OS browser — both auto-detected URLs (`WebLinksAddon` custom handler) and explicit OSC 8 hyperlinks (xterm `linkHandler`), routed to `openTab` (web schemes only; file:/mailto: left alone). _Note:_ on the shipping xterm.js terminal. |
| 4 | ✅ | **`flux` CLI inside the terminal** (done): `flux url|title|dom|links|extract-json` read the active page (`extract-json` → JSON for `jq`). Flux writes the active browser tab's context to `$FLUX_RPC_DIR/active.json` (`rpc.rs`, refreshed on dom_publish + tab switch; private tabs withheld); the CLI (`cli.rs`) reads it. File-based (not a socket) so it crosses the WSL↔Windows boundary via `WSLENV …/p`. _Remaining:_ the `flux` binary must be on the terminal's PATH (Linux build in WSL); native-Windows-console stdout needs AttachConsole (`windows_subsystem` swallows it). |
| 76 | ✅ | **Bundle a Nerd Font** (done): CaskaydiaCove (Cascadia Code) Nerd Font Mono — Regular+Bold woff2 (~2.4 MB) in `apps/shell/public/fonts`, `@font-face "CaskaydiaCove NF"` in theme.css, prepended to the terminal `fontFamily`. Guarantees Private-Use-Area icon glyphs without a system install. (`public/fonts/.gitignore` blocks re-dropping the source zip.) |
| 74 | P2 | Terminal throughput: raw-byte channel transport (avoid `number[]` JSON) and/or route the PTY stream into the `flux-term` WGPU renderer when compositing is solved |
| 75 | ✓ | Terminal **splits within the column** (done) — multiple PTY panes side-by-side or stacked (persisted), hover toolbar to split / flip / close the focused pane, click-to-focus (teal ring), **draggable seams to resize**. Each pane its own shell with OSC 133. The column stays up alongside a terminal *tab* too. Also done: OSC 8 link → Flux browser tab (#17), OSC 133 integration (#16). In-column *tabs* intentionally skipped — the app's Terminal tabs cover that. _Possible follow-up:_ keyboard-driven split/focus (needs routing through xterm's key handler). |
| 98 | ✓ | **Terminal persistence, both halves** (done) — a Settings toggle (no longer env-only) with two independent modes. _Live:_ the shell is handed to **`dtach`** (preferred — one job, no config, and no second terminal emulator behind xterm.js) or tmux, so processes survive a Flux restart; dtach has no kill verb, so an explicit tab close finds its master by a `dtach`-anchored pattern over the session's unique socket (the bare path alone also matched unrelated processes, and a `sh -c` wrapper matched *itself*). _Transcript:_ output is recorded to a 256 KB-capped per-session file and replayed on reopen — needs nothing installed, works on native Windows, and unlike the live half survives a **crash or reboot**, which is what this entry originally asked for. Off by default: the transcript writes terminal output to disk. _Remaining:_ nothing planned; a resurrect-live-sessions-on-boot hook is still impossible for the live half by construction (the broker dies with the machine). |

## Epic: Flux Agent

| # | P | Item |
|---|---|---|
| 1 | P2 | (Optional) in-process `llama-cpp-2` path for a single self-contained binary — the agent ships via Ollama now (ADR 0005), so this is no longer P0; GGUF load + GBNF sampler behind the `llama` feature |
| 81 | ✅ | **Agent model picker** (done): the agent-sidebar header shows the active model and opens a dropdown of locally-pulled Ollama models (`/api/tags`); picking one switches the agent live (runtime override over `FLUX_MODEL`) and persists. _Follow-up:_ show which model is active in the sidebar per-message; per-task model routing. |
| 8 | ✅ | **Agent action confirmation + multi-step tasks** (done): `/act` **plans then previews** one action (agent_plan) and waits for **Approve / Skip** before anything touches the page. **`/task <goal>`** (#A) runs the **iterative agent loop** — plan next step from the live page + history → Approve/Skip/Stop (or "Run all") → execute → re-plan, until `finish`/`refuse`/8-step cap. Closed `AgentAction` vocab → audited JS; destructive clicks blocked at click-time (#104) even in Run-all. Plans are now **JSON-Schema-constrained** (Ollama `format` = the `AgentAction` schema) and chat **streams token-by-token** to the sidebar (was #82, done). |
| 115 | ◐ | **Agent integrations trio.** _Done:_ **AudioPulse / Spotify control** — the agent panel parses music intents ("play …", "skip", "pause", "resume", "what's playing", optionally "hey gemma, …") and drives the Spotify Web API by **reusing AudioPulse's cached token** (`~/.config/audiopulse/token.json`, Path A — no AudioPulse changes); token refreshed on demand in-memory (`spotify.rs`). **Lens** (done): "what is this?" / "/lens" / ⌘K "Identify page" captures the active page (#54 `CapturePreview`) → `gemma3:4b` (or `FLUX_VISION_MODEL`) over Ollama `/api/generate` images → identification in the agent panel (`lens.rs` + `agent_lens`, fully local). **Push-to-talk voice** (done): a 🎤 on the agent input — hold to talk, release to transcribe offline with **Vosk** (reuses AudioPulse's model; mic captured in-browser → 16 kHz PCM → `voice_transcribe`), text lands in the input. Behind the `voice` cargo feature (loads native `libvosk` at runtime from PATH, `FLUX_VOSK_LIBRARY`, or `FLUX_VOSK_LIB_DIR`); `FLUX_VOSK_MODEL` override. _Voice follow-ups:_ auto-send option; **"Hey Gemma" always-on wake-word** (deferred — always-on mic). _Lens follow-ups:_ region-select crop; stream the answer. **Multi-step chains** (done): a compound request ("read foo.rs then fix the bug then run the tests"; "play my liked songs + shuffle on") is decomposed into Flux-formatted sub-commands (`agent_plan_steps` → `plan_steps`) and each is routed through the same tools as a typed message, in order (`runChain`/`routeChainStep`); edit/shell steps surface their normal approval card and the chain pauses (shared `chainGate`) until Apply/Run (continue) or Cancel (abort). Typed or voice; gated on a connector + a real first action so single commands/chat fall through. **Adaptive fix loop** (done): "/fix &lt;goal&gt;" runs an iterative loop (`agent_next_step` → `plan_next_step` + `runAdaptiveTask`) — plan ONE step, route it (capturing the result via the shared `chainGate`/`StepOutcome`), feed the result back, re-plan; reacts to failures (run → read the failure → edit → re-run) until done/stuck or a 10-step cap; each edit/command still approval-gated. _Remaining:_ a distinct chain/fix stop control (reuses the page-task bar, whose "Run all" is inert here); a broader Spotify tool surface (shuffle/repeat/volume/playlists/launch-app); voice trigger for /fix (typed-only, since the loop needs on-screen approvals). |
| 135 | ◐ | **Agent domain playbooks / harnesses** (done: Power Platform): `flux-agent::playbooks` injects a domain-specific recipe into the planner prompt (`plan`/`plan_step`) when the active page host matches a known hard-to-navigate app — shipped for **Power Automate** + **Power Apps** (create→trigger→add-step→configure→save→test recipe, aria-label-first selectors, verification gates, and a STOP list that refuses OAuth/connection/credential/publish/delete and points bulk Dataverse work at `pac`/Dataverse MCP). Host match is registrable-boundary (no lookalike bypass); generic pages get an empty block (prompt unchanged); harness never widens the `AgentAction` vocab. _Follow-ups:_ (a) ground `chat` too, so "how do I add a delay action?" on the portal is playbook-aware; ~~(b) wrap the **`pac` CLI**~~ **(done)** — `/pac <request>` maps NL → one `pac` command via `flux-agent::pac` (cheatsheet-grounded), Rust-classified risk banner, `pac_status` preflight, runs through the approval-gated shell card; (c) add the **Dataverse MCP** as an opt-in remote connector (Entra token in keyring, per-env profiles); (d) more playbooks (Azure portal, GitHub, Jira); (e) let playbooks contribute a few known-stable selectors, not just prose. _Next:_ (c). |
| 11 | ◐ | **Real embeddings (EmbeddingGemma)** (done for search): `embedding.rs` prefers Ollama `/api/embed` (`embeddinggemma`, `FLUX_EMBED_MODEL`) with the hashing embedder as automatic fallback (search never breaks offline). Vectors persisted + embedder-tagged; an embedder change triggers a background re-embed (`migrate`). Wired into the **archive** (#69). _Remaining:_ Omni (#66) + clustering (#14) still use hashing — they embed per-keystroke / on every dom_publish, so they need a vector cache first; archive_save/search are sync (brief network block when the model path is active) — make async; int8/quantized model guidance. |

## Epic: Tabs & Chrome import

| # | P | Item |
|---|---|---|
| 6 | ✅ | **Command palette** (done): `Ctrl+K` glass modal — fuzzy search over open tabs (switch), actions (new tab/term/files, toggles, open History/Passwords/Omni, find, reload, close), and history. Keyboard-driven (↑/↓/Enter/Esc). Hides the active webview while open (OS-layer overlay). Completes the #18 keymap. _Follow-up:_ bookmark search once #22 lands. |
| 18 | ✅ | **Keyboard shortcuts** (done): Win/Linux Ctrl-based (Cmd accepted on macOS). New browser tab `Ctrl+T`, terminal tab `Ctrl+Shift+T`, close tab `Ctrl+W`, next/prev tab `Ctrl+Tab`/`Ctrl+Shift+Tab`, jump to tab `Ctrl+1‑9`, toggle terminal `` Ctrl+` ``, toggle agent `Ctrl+Shift+A`, toggle sidebar `Ctrl+B`, focus omnibox `Ctrl+L`, reload `Ctrl+R`/`F5`, back/forward `Alt+←`/`Alt+→`. (Deviated from the original ⌃A/⌘S spec to avoid the Ctrl+A select-all / Ctrl+S save-page collisions on Win/Linux.) Works both when the chrome is focused (capture-phase listener) and when a page webview is focused (injected `shortcuts.js` forwards chords via the `chrome_key` fluxtab command). Terminal-focus guard leaves readline/tmux chords (Ctrl+R/W/L/B) to the shell. `Ctrl+K` opens the command palette (#6). |
| 28 | ✅ | **Responsive layout** (done): a `responsive` memo over a tracked window width sheds panes in priority order when the fixed columns would push the content card below `MIN_CONTENT` — terminal, then web panel, then agent, then the sidebar collapses to its icon rail — restoring them as the window grows. Non-destructive (the open/closed signals stay set; only the rendered layout adapts); drives `columns()` + the per-pane render/splitter gates. _Remaining:_ auto-collapsing the agent mid-task unmounts its panel (loses the feed) — same as a manual toggle, but only triggers at very narrow widths; thresholds are a fixed heuristic, not user-tunable. (ADR 0002 mitigation.) |
| 79 | ◐ | **Performance pass** (user priority — esp. **low RAM** vs Chrome). _Done:_ hardened release profile (`lto=fat`/`codegen-units=1`/`panic=abort`/`strip`); **lazy webviews**; **DOM snapshot cache capped** (1 MiB/256 KiB); **tab hibernation #45**; idle RAM poll gated to when Settings is open (was an always-on 2.5 s timer); **glass `backdrop-filter` dropped during resize/drag** (`body.resizing`/`.busy` → `--glass-blur:none`; pane-resize, window-resize, split/panel-drag) — the GPU-heavy per-frame re-sample is the main resize jank. _Adblock engine verified:_ `shields_check` is unused (no IPC-pool spread); the serialized rules are one shared `Arc`, only the deserialized `Engine` is per-thread, bounded to ~2 (WebView2 interceptor + page-load cosmetic). _Remaining:_ collapse those two to one engine via a worker thread (needs the request hot path measured — deferred); runtime latency profiling on the user's machine; measure vs ADR 0001 budgets; the webview-placement bug. |
| 29 | P2 | Address bar does in-place navigation of the active tab (depends on #2); ⌘L focuses it |
| 30 | ✅ | **Tab drag-and-drop reorder** (done): drag tabs in the strip to reposition (drop above/below by cursor half); order is an explicit **persisted** backend sequence (`tab_reorder`), survives restart. _Follow-up:_ reorder within the pinned grid + drag into/out of it. |
| 21 | ✅ | **Favicons** (done): the tab strip + pinned rail show each site's real favicon. Fetched **cookielessly, directly from the site** (never a 3rd-party favicon service) by a Rust command — `/favicon.ico` then the declared `<link rel="…icon">`, with magic-byte image validation (rejects soft-404 HTML) — and cached **per host on disk** as a `data:` URL. Letter glyph remains the fallback. Unit-tested. _Follow-up:_ surface favicons in history/omnibox once those land. |
| 22 | ✅ | **Bookmarks** (done): persisted folder-aware store (`bookmarks.rs`), `flux://bookmarks` page (search + folder grouping + delete + clear), footer 🔖 popover ("★ Bookmark this page", All bookmarks, history), ⌘K "Open Bookmarks". **Chrome import** pulls every bookmark from a chosen profile under an "Imported" folder (de-duped). Each folder can be **"⊞ Open as group"** → opens its bookmarks as tabs in a new Flux tab group (capped at 20) — the practical bridge for Chrome tab groups. A **bookmark bar** (chip row docked under the content card, toggle via ⌘K, persisted) gives one-click access without opening the page; **rename** a bookmark inline (double-click a chip / ✎ on the page → `bookmark_rename`). _Follow-ups:_ folder tree editing, omnibox bookmark autocomplete (#32), drag-to-reorder (bar + page). |
| 23 | P1 | Chrome **saved tab groups** import: parse the `Saved Tab Groups` SQLite db (needs `rusqlite`; schema is sync-internals, version-fragile — pin per Chrome milestone). _Note:_ only present when the user uses Chrome's *saved* groups; live (unsaved) groups are in the undocumented SNSS session blob. The #22 "open folder as group" covers the common case meanwhile. |
| 24 | P2 | Extension import UX: resolve `__MSG_` locale names from `_locales/`, map common extensions to Flux built-ins (uBlock → native content blocking, dark-reader → theme override). Chrome extensions **cannot run** in native webviews — this is inventory + equivalents, never execution |
| 25 | P2 | Import from Chromium variants: Chromium, Brave, Edge (same formats, different user-data roots) |

---

# Feature roadmap — competitive parity & differentiation

Surveyed against Chrome, Edge, Safari, Firefox, Arc, Zen, Vivaldi, Brave, and
Opera (2025–26). Split into three buckets: **table stakes** (a browser is not
credible without these), **best-in-class** (the features that make power users
switch), and **under-served** (highly requested, poorly served by the
incumbents — Flux's wedge). Numbers continue the stable sequence.

## Epic: Browser core — table stakes

Flux currently has tab *chrome* but no actual web engine wired (BACKLOG #2).
These are the non-negotiables before Flux is a usable daily browser.

| # | P | Item | Who has it |
|---|---|---|---|
| 31 | ✅ | **Navigation polish** (done): **stop** (`webview_stop` → `window.stop()`; reload button swaps to ✕ while loading + `Esc`); **loading state** tracked from the page-load events (started/finished) per tab; **security/TLS badge** left of the omnibox (🔒 HTTPS / ⚠ HTTP); **progress affordance** — an indeterminate bar under the omnibox (in the sidebar, since the native webview overlays the content card and would hide an in-card bar). | all |
| 32 | ✅ | **Omnibox live suggestions** (done): type-ahead dropdown — local **history** matches (with favicons) + **search suggestions** from the default engine's `suggest_template` (`search_suggest` fetches + parses OpenSearch JSON). Keyboard-navigable (↑/↓/Enter/Esc), sidebar-resident. A Settings toggle (default on) gates the remote fetch for privacy; history suggestions stay local regardless. _Follow-up:_ bookmark autocomplete once #22 lands. | all |
| 33 | ✅ | **Find-in-page** (done): `Ctrl+F` opens a find bar (sidebar-resident — the native webview overlays the content card); typing drives the engine's native `window.find()` (Chromium + WebKit) for highlight + scroll, Enter/Shift+Enter (and ‹ ›) step next/prev, and a case-insensitive **match count** is reported back over `flux://find-result`. Esc closes. _Follow-up:_ precise current/total index + highlight-all (window.find gives native single-match highlight only). | all |
| 34 | ✅ | **Download manager** (done): WebView2 `DownloadStarting` interception → live progress + state, Flux-owned UI (footer ⬇ popover w/ active badge, progress bars). Controls: pause / resume / cancel (live COM ops via `run_on_main_thread`), open / show-in-folder. Model unit-tested; COM compile-verified vs msvc. **WebKitGTK hook done (compile-verified):** `download-started` on the shared `WebKitWebContext` (hooked once per context) routes downloads to the OS Downloads dir with numbered dedup, feeds the same `DownloadState` + `flux://download-updated` progress events, and supports cancel; pause/resume stay Windows-only (WebKitGTK has no pause API). _Follow-ups:_ Linux runtime smoke test, integrity hash. | all |
| 35 | ◐ | **Built-in PDF viewer** (done): PDFs open in a Flux-owned PDF.js viewer (`flux://pdf`) — continuous pages, zoom, page count, download — cross-engine (WebKitGTK has no native viewer). `.pdf` addresses, ⌘K, `target="_blank"` PDF links, and opening a PDF in the file explorer route here; bytes fetched by `pdf_fetch` (server-side, CORS-free, base64, 32 MB cap; http(s)/file://); PDF.js lazy-loaded so the chrome bundle stays lean. _Remaining:_ annotation + form-fill; in-page link clicks to PDFs still use the engine's native viewer on WebView2 (would need request interception to reroute). | all |
| 36 | ✅ | **Per-site zoom** (done): `Ctrl +/-/0` zoom the active page (`webview.set_zoom`); the factor is **persisted per host** (localStorage) and re-applied on every load. A `%` pill in the address row appears when ≠ 100% (click to reset); also in ⌘K. | all |
| 37 | ◐ | **Picture-in-picture** (done): hover a sizable `<video>` for a ⧉ PiP button or press Alt+P to pop it into a floating window (toggle to exit); best-effort auto-PiP when the tab is backgrounded. Injected `pip.js` triggers from in-page gestures because `requestPictureInPicture` needs in-page user activation (a chrome button/eval gets `NotAllowedError`). _Remaining:_ a Settings toggle; reliable auto-PiP (needs the engine to fire `visibilitychange` on a hidden child webview + live transient activation); WebKitGTK PiP support. | Chrome/Arc/Safari |
| 38 | ◐ | **Permissions UI** (done): **`flux://permissions`** (+ ⌘K, Shields "Manage site permissions…") — a per-site, per-kind manager for camera / microphone / location / notifications / clipboard. Allow / Block / Ask per site or add a rule manually; persisted (`permissions.json`). A remembered Allow/Block is applied automatically on WebView2 (`PermissionRequested`, reading kind + host), short-circuiting the native prompt; the global block (#58) overrides. Policy resolution is unit-tested. **Flux-styled Ask prompt done** — the WebView2 request is deferred (`GetDeferral`, main-thread pending registry), `flux://permission-ask` raises a glass **permission bar docked above the content card** (Allow / Block / dismiss-once + "Remember for this site"), and `permission_answer` completes the deferral; COM surface compile-verified vs msvc. _Remaining:_ the WebKitGTK `permission-request` signal on the Linux backend. | all |
| 39 | ✅ | **History** (done): persisted, searchable browsing history at `flux://history` — auto-recorded from the DOM-capture pipe (URL + page title), frecency-ranked, capped + debounced-save. Full-page view: recents grouped by day, live search, per-row favicons (#21), open / remove-one / clear-all; reachable from Start page + 🔖 Library. Store unit-tested. _Follow-up:_ a calendar view. | all |
| 40 | ◐ | **Translate page** (done): 🌐 page-action (+ ⌘K "Translate page → …") translates the active page with the **local model** (`AgentPlanner::translate` → Ollama; private, no cloud), shown in the reader overlay as paragraphs. 🌐 → your own language (`navigator.language`); ⌘K offers common targets. _Remaining:_ translates only the leading visible text (model context cap) and renders as clean text, not layout-preserving in-place. | Chrome/Edge/Safari |
| 41 | ✅ | **Reader mode** (done): 📖 in the address row (+ ⌘K) extracts the article (injected `reader.js` density heuristic → structured blocks posted via `reader_publish`) and renders a decluttered, typographic view over the hidden webview. **Text-to-speech** via the Web Speech API (🔊 Listen). Blocks render as text + `<img src>` only — no raw HTML, no XSS. Esc/✕/tab-switch closes it. **Structural reading done:** deterministic outline rail (heading blocks, click-to-jump) + Gemma-classified section chips (paper→Abstract/Methods/Results, recipe→Ingredients/Steps, docs, news; `structure_reading` schema-constrained + Rust label-allowlist validation; chips absent when Ollama is down, outline stays). _Follow-up:_ richer extraction (Readability-grade), per-paragraph TTS highlight, font/width controls. | Safari/FF/Edge |
| 42 | ◐ | **Install site as app / PWAs** (done): ⌘K "Install this site as app" opens the site in its own Tauri `WebviewWindow` (just the page); installed apps persist (`pwa.rs`) and live at **`flux://apps`** (relaunch focuses the existing window, remove). _Remaining:_ a real dock/taskbar icon per app (favicon → window icon) + a `.desktop`/Start-menu shortcut (ties into packaging #26); chrome-less custom title bar. | Chrome/Edge |
| 110 | ✅ | **Devtools (F12)** (done): F12 opens the inspector for the active tab's webview, whether the chrome or the page is focused (Tauri `devtools` feature + `webview_devtools` command + `shortcuts.{ts,js}` mapping). | all |
| 109 | ✅ | **New windows / "open in new tab"** (done): `window.open()`, `target="_blank"` links, and middle-click / Ctrl-(⌘-)click on any link open as new Flux tabs — previously a silent no-op since native child webviews ignore page-initiated windows. Injected `newtab.js` → `chrome_open_url` (fluxtab cmd) → `flux://open-url` → frontend `openTab` (background for middle/modifier clicks, foreground for explicit `_blank`). **Right-clicking a link** also shows a small Flux menu (open in new / background tab, copy link) rendered in a shadow root in the page — the native right-click "open in new tab" fired a new-window request Flux can't host, so it was a silent no-op. _Follow-up:_ a *connected* popup window with a live opener (OAuth / `postMessage` handshakes) needs WebView2 `NewWindowRequested` / WRY new-window hosting — separate, larger, and Google blocks embedded-webview OAuth regardless. | all |

## Epic: Best-in-class power features

The features that make Arc/Vivaldi/Zen users evangelical.

| # | P | Item | Inspiration |
|---|---|---|---|
| 43 | ✅ | **Split view** (done): tabs tiled side by side in the content card, with a draggable seam (clamped 20–80%, double-click to even). Start via right-click → "Split with current tab" or drag a tab onto another's right edge. Pauses when a third tab is focused, resumes on return; both panes stay live (excluded from hibernation) and re-tile on resize. Built on the child-webview model — the seam is a DOM splitter in the inter-pane gap, and dragging hides the panes so the chrome can read the pointer. **Windows-style tiling** (done): up to 4 panes in 7 preset layouts (columns/rows/quad/main-left/right/top/bottom), with one pure geometry function (`tiles.ts`) shared by the pixel-space webview tiler and the percentage-space DOM panes, persisted with the session. **Several groups coexist** (A│B and C│D both remembered, as the original pair model allowed — the 4-pane rework had collapsed this to one at a time). **Flux's own pages tile from their own toolbar** (the ◫ was gated on "is a real web page", so the Trail/Omni/Scribe/Settings/start/Files had none, and they couldn't be picked as a pane either). **Terminal tabs tile too** (done): the keep-alive layer (#73) is positioned into its slot rather than filling the card, with a `visible` prop separate from `active` so a tiled terminal re-fits without stealing focus, and only the active one drawing the WebGL backdrop (#75). | Arc, Edge, Vivaldi, Zen |
| 44 | ✅ | **Workspaces** (done): Arc-style named/colored tab spaces, each with its own tabs (+ pinned + groups, filtered to the active space). Switcher above the tools (switch / + / rename / recolor / delete). **RAM-optimized**: switching away destroys the leaving space's webviews + lazy creation → inactive spaces cost only KB of metadata. Persisted (`Workspace` model, per-tab `workspace`, active). **Send a tab or a whole group to another space** via right-click. _Follow-ups:_ per-space theme tint, cluster-based auto-assignment. | Arc, Opera, Zen, Edge |
| 45 | ✅ | **Tab hibernation / sleeping tabs** (done): background browser tabs idle past a timeout have their **native webview destroyed** (RAM freed); the tab stays in the strip (dimmed + 💤) and the page reloads when re-activated (reuses the lazy-webview path). On by default, 30 min, configurable in Settings (on/off + 5/15/30/60 min). Active + start/terminal/files tabs never sleep; no clear-on-close on hibernate. **Scroll position + non-password form state preserved** across sleep (captured on switch-away to a RAM-only store, re-applied on the wake reload; passwords never captured, nothing on disk). **Memory-pressure eviction**: reads actual system memory (`sysinfo`) and sleeps the LRU background tabs when free RAM is low (<12%), adapting to the machine; Settings shows a live RSS/free-RAM readout. | Edge, Vivaldi, Brave |
| 46 | ✅ | **Auto-archive** (done): Settings → "Auto-archive stale tabs" (Off by default; 1–30 days). Browser tabs untouched for N days are closed into a restorable **Archived Tabs** list (🗄 footer panel, shown once non-empty; one-tap reopen). Last-access keyed by URL in localStorage (survives restarts; restored tabs seeded as fresh); the sweep runs inside the existing hibernation timer (≤5/min), excluding pinned/foldered/active/start tabs. Store: `staleTabIds`/`archiveTabRecord`/`archivedTabs`/`restoreArchived`. **Branch-aware (rabbit-hole) upgrade done:** stale tabs Trail-connected to each other archive as ONE named unit (`trace_branches` union-find over visits+edges w/ newest-visit-by-URL fallback; Gemma names the branch async; 🌿 rows with member chips + restore-all in the panel; sweep lazily imported off the boot bundle; one branch + ≤5 singles per sweep). _Remaining:_ the URL→time access map isn't pruned (grows with history); no "archive now" manual action; doesn't store page content (reopen re-fetches), unlike the read-later archive (#69). | Arc |
| 47 | ✅ | **Session management** (done): save the current tabs as a **named session**, then restore (reopens every tab) from `flux://sessions` (+ ⌘K "Open Sessions", Library popover link). Persisted separate from the always-on session (#19). **Automatic daily snapshots** (#62-adjacent): a background timer buckets the open tabs by day (keeps a week) into `snapshots.json`; the Sessions page shows a "Recent days" list (Today / Yesterday / weekday) with one-click **Reopen** — "reopen yesterday" without having saved anything (`SnapshotStore`, `snapshots_list`/`snapshot_restore`). _Follow-up:_ restore into a fresh workspace. | Vivaldi, Opera |
| 48 | ✅ | **Web panels** (done): pin a site (chat, docs, music) to a slim pane on the right of the content card, persistent across tab switches. Footer ◨ popover: pin this page / toggle / unpin (persisted). Draggable divider; DOM toolbar (title / reload / close). Its own native webview (no capture.js → never pollutes history/clustering); only the open panel is live (RAM-conscious). **Unread badges**: injected `panel-badge.js` parses the page title's `(N)` count (Discord/Proton/LinkedIn/Gmail convention) and reports it via the `panel_badge` bridge → a phone-style red bubble on the rail icon (`flux://panel-badge`; a new `capabilities/panel.json` finally grants panel webviews the `fluxtab` bridge, which also makes shortcuts.js's chrome-chord forwarding work in panels). Live for the open panel; others keep their last-known count until reopened. _Follow-ups:_ an opt-in "keep pinned panels live" mode so all badges update without opening each (RAM tradeoff); multiple concurrent panels, left-side option, per-panel zoom. | Vivaldi, Opera, Edge |
| 49 | ✅ | **Agent boosts** (done): footer **✨ Boosts** popover — describe a change and the local agent writes per-site CSS (`AgentPlanner::author_css`), saved per host (`boosts.rs`) and injected on every page-load (+ live re-inject on author/toggle). Toggle/delete per site. **Hand-editable CSS** (done): an AI/CSS authoring toggle (paste raw CSS, saved via `boost_save`) + an ✎ inline editor on each boost. **Manage-all** (done): an "All sites" view lists/toggles/edits/deletes every boost across hosts (`boosts_list`). **Subdomain/wildcard matching** (done): `host_matches` — a base-domain boost spreads to subdomains (`github.com` → `gist.github.com`), a subdomain-specific boost stays scoped, a leading `*.` is accepted, and suffix-spoofs (`github.com.evil.com`) are rejected; unit-tested. CSS-only by design (raw JS deliberately not exposed). | Arc, Vivaldi, Stylus |
| 50 | ◐ | **Peek / glance / little-window** (done): open a link in a transient, always-on-top **floating window** (`peek-<n>`, its own `WebviewWindow`) without committing it to a tab. A separate window (not a DOM overlay) is the right model under the native-webview overlay constraint. Triggers: **Alt-click** a link or right-click → **Peek** on real pages (`newtab.js` → `chrome_peek_url`), and **Peek** in the internal-page link menu (`peek_open`). Inside the peek, injected `peek.js` adds a floating bar — **⊕ Open as tab** (promote the current page to a real focused tab + close; also **Ctrl/⌘+Enter**), **📌 Pin** (drop always-on-top so it stays open as a normal kept window), **✕ / Esc** to dismiss — over the `fluxtab` bridge (`capabilities/peek.json`); `peek_promote`/`peek_pin`/`peek_close` are Rust-guarded to `peek-*` windows so a normal page can't act on the main window. Promote also re-focuses the main window. **Shields apply to peeks too**: the same WebView2 request interceptor (content blocking + HTTPS-only + lean, #57/#91/#105) is installed on the peek window (`netfilter::install_on_window`) and cosmetic element-hiding CSS is injected on load — peeks are a separate `WebviewWindow`, not a `tab-*` child, so this is wired explicitly. _Remaining:_ an in-card "little window" variant; verify on Windows/WebView2 (built + unit-tested; the WebviewWindow + capability + interceptor path needs an on-device run). | Arc, Zen |
| 114 | ◐ | **Calendar + Tasks home widgets** — shipped: calendar syncs any calendar **read-only via its secret ICS URL** (`calendar.rs`, no OAuth) with event dots + an upcoming list; a local **Tasks** widget (`todos.rs`, on-device). _Remaining:_ **Google Tasks two-way sync** + Google Calendar write — needs the desktop OAuth loopback flow (PKCE, system-browser login, refresh token in the vault) **and a user-created Google Cloud OAuth client + enabled APIs**; deferred (can't runtime-verify OAuth in the WSL dev env). Recurring-event **RRULE expansion** ✓ (ICS feeds **and** local events), a **Google-Calendar-style week/day time grid** ✓, **editable + repeatable local events** ✓ (add/edit/delete/drag + a Repeat control → Daily/Weekly/2-weekly/Monthly/Yearly/custom RRULE, expanded over the grid window, 🔁 marked; on-device store) and **Gemma read/write control** ✓ ("what's on my calendar", "schedule … tomorrow at noon", "move/cancel …") now shipped. **Calendar-from-anywhere** ✓ — footer 📅 popover (mini month + upcoming agenda, lazy body), 30-min upcoming-event dot, ⌘K "Open calendar". _Remaining:_ ICS timezone (TZID) handling; **per-occurrence exceptions** for a recurring local series ("this event only" edit/delete — today they share the series id); two-way **Google write-back** (OAuth, deferred). | Arc/Vivaldi widgets |
| 113 | ◐ | **Advanced PDF editing** — markup + page-ops (#112) and **AcroForm form fill** shipped: 🖊 Forms mode fills fields **in place on the page** (+ a synced side panel), Apply/Flatten/Save. **OCR for scanned PDFs** shipped: a document with no text layer says so and offers **Read with OCR**, which renders each page at 2x and runs the local `tesseract` binary, indexing the result under its own `pdf-ocr` source so citations mark it machine-read. _Remaining:_ true **in-place text editing** (reconstruct text runs — inherently fragile), cryptographic **digital signatures**, redaction-with-guarantees | Adobe Acrobat |
| 51 | ✅ | **Mouse gestures** (done): hold right button + drag — left = back, right = forward, down = reload, up = top; suppresses the context menu after a gesture. Settings toggle (off by default). _Follow-up:_ rocker gestures + rebindable. | Vivaldi, Opera |
| 52 | ✅ | **Vim link-hints + scroll** (done): `f` labels clickable elements (type the label to click), `j`/`k` scroll, `gg`/`G` top/bottom; never fires while typing in a field. Injected `nav.js` gated by a Settings toggle (off by default). _Follow-up:_ `F` open-in-new-tab, visual-mode, more motions. | Vimium users |
| 53 | ◐ | **Per-page notes** (done): a footer 📝 popover holds a note tied to the current page's URL, auto-saved locally (`notes.json`) and restored on revisit (blank clears it). _Follow-up:_ a notes index page, host-level notes, on-page highlight/annotation anchors. | Arc, Vivaldi |
| 54 | ✅ | **Web capture** (done): 📸 in the address row (+ ⌘K "Capture page") saves the visible page to a PNG in `app_data/screenshots` via WebView2 `CapturePreview` (COM verified vs msvc); a toast confirms when written. _Remaining:_ full-scrolling-page stitch, region select, annotation, and a WebKitGTK snapshot path (Windows/WebView2 only today). | Edge, Firefox |
| 55 | ✅ | **Focus / compact mode** (done): `Ctrl+Shift+F` (or ⌘K "Focus mode") hides the sidebar, terminal, and agent — content only. `Esc` or the chord exits (a toast reminds you on enter). | Zen, Arc |
| 56 | ✅ | **Tab groups** (done): named/colored/collapsible groups in the strip. Right-click → pin / new group / add-to / remove / close; headers collapse, rename (dblclick), recolor (dot), ungroup (✕). **"⊞ Group"** = group-by-topic, seeded from the semantic clusters (#14). Persisted (`TabGroup` model + commands). _Follow-up:_ drag tabs between groups (today grouping is via the menu; drag only reorders). | Vivaldi, Chrome groups |
| 111 | ✅ | **Tab folders** (done): a collapsible **Folders** section above the footer parks tabs that are kept **hibernated (≈0 RAM)** — distinct from groups (strip-resident, colored). Right-click a tab → *Move to folder* / *+ New folder with tab*; members leave the strip and sleep. Clicking a folder tab wakes + views it; switching away re-sleeps it (a `createEffect` keeps every non-active folder tab hibernated). `TabFolder` model + `folder` tab field + commands; persisted. The Folders section lays its folders out **horizontally as compact columns** (scrolls sideways), so adding folders grows it sideways rather than down; members stay a vertical list inside each column. _Follow-up:_ drag tabs into folders; per-folder "wake all". | Arc folders |

## Epic: Privacy, security & data

**Active track** (chosen 2026-06-15): #91 → #57/#60, #58, #59, #61. Because Flux
uses *native* webviews (WebView2 / WebKitGTK), not a Chromium network stack, the
implementation path differs from a normal browser — captured per item below.
**#91 is the enabling primitive** the blocker and HTTPS-upgrade both sit on.

| # | P | Item | Inspiration |
|---|---|---|---|
| 91 | ◐ | **Request-interception layer** (ADR 0007): engine + policy + the **WebView2 interceptor** are wired and **verified blocking on Windows** (`with_webview` → `WebResourceRequested` → `403`). **WebKitGTK path done (compile-verified):** shields exports its lists as WebKit content-blocker JSON (`flux_filter::to_content_blocker_json`, seeded from the bundled list at boot, refreshed with EasyList), compiled once per run via `UserContentFilterStore` FFI and attached to every webview — blocking runs inside WebKit itself. _Remaining:_ Linux runtime smoke test; the declarative path can't feed the tracker graph or HTTPS-only upgrades (no per-request callback) — needs a web-process extension if ever wanted. Reused for #58's http→https rewrite (Windows). | enabling primitive |
| 57 | ✅ | **Built-in content blocker** (done): `flux-filter` (Brave's `adblock`) + WebView2 interceptor block at the request level (verified on Windows); full EasyList + EasyPrivacy fetched/cached; shields UI (footer badge + global/per-site toggle + update); **cosmetic element-hiding** (per-page injected CSS, all backends). Minor follow-up: user-supplied custom filter lists. | Brave, Vivaldi, Opera |
| 58 | ✅ | **HTTPS-only + cookie controls** (done): http→https upgrade + per-site allow-HTTP; cookie clear (site/all) + clear-on-close; native tracking prevention (Off/Basic/Balanced/Strict); block camera/mic/geo toggle. All via the #91 interceptor / WebView2 profile, Shields-popover controls, COM verified vs msvc. (Skipped: a downgrade interstitial — Allow-HTTP recovers — and Flux-styled *remembered* permission prompts — WebView2 prompts by default.) | Brave, Firefox |
| 59 | ✅ | **Multi-account containers / profiles** (done): **private/incognito tabs** (`incognito` webview — in-memory, no history, ephemeral) **and named persistent containers** — each container gets an isolated cookie/storage jar via a per-webview `data_directory`, so you can be logged into two accounts of the same site. Create/rename/recolor/delete in Settings; "Open in container ▸" in the new-tab picker; container color marks the tab's rail; persisted. _Follow-up:_ move an existing tab to a container (needs webview recreation). | Firefox, Safari profiles |
| 60 | P2 | **Fingerprint randomization** + tracker-script blocking (script blocking falls out of #57; fingerprint defenses via injected JS shims over canvas/WebGL/audio). | Brave |
| 61 | ✅ | **Password manager + autofill** (done; ADR 0009). `flux-vault` (AES-256-GCM, Zeroizing, host matching, **Proton Pass importer** — CSV/ZIP/PGP/JSON, format-detected, unit-tested) + OS-keychain data key (`keyring`, file fallback) + persistence; flux-core commands (status/list/for-host/reveal/add/remove/import-proton/fill); **autofill** injected straight into the page (same-origin enforced; password never touches chrome JS); footer 🔑 vault UI (list + per-site Fill + copy/reveal/delete + Proton import + add). **optional master password** (Argon2id-wrapped data key, removed from the keychain) + **idle auto-lock** + manual lock. Passkeys/WebAuthn handled by the native webview. **Page sentinel done (passwords.js):** detects registration forms (autocomplete="new-password" / password+confirm / signup wording) and offers an in-page "✦ Use a strong password" chip — vault-generated (OS-CSPRNG, rejection-sampled, 20 chars, unit-tested), filled into every password field, **saved to the vault on sign-up submit** (host from the webview label, never page-trusted) with a chrome toast; on login forms with a saved match, a "🔑 Fill · user" chip one-click-autofills via the existing Rust→page path. Top-level document only (no iframes); vault must be unlocked. **Save-password prompt done (passwords.js + save bar):** submitting a login/sign-up form with a password Flux didn't fill or generate captures it and raises a chrome **"Save password for host?"** bar (Save/Update/Not now/**Never** per-site) — password held only in Rust between submit and answer, host label-derived, never-save list persisted (`vault_offer_save`/`vault_save_confirm`/`vault_save_dismiss`/`vault_never_save`). **Credential picker done:** the 🔑 chip expands to an in-page username list when >1 login matches, filling the chosen one via host-validated `vault_fill_page_id` (`vault_page_matches`). **Chrome + Bitwarden import done:** the importer auto-detects Chrome CSV, Bitwarden CSV, and Bitwarden JSON (unencrypted) alongside Proton; encrypted Bitwarden exports error clearly (4 tests). _Note:_ Proton ships only a WebExtension + no public API → import is a snapshot (re-export to resync). _Follow-ups:_ **1Password importer** (1pux/CSV); a no-form (XHR-login) save prompt — the save capture keys on a real `submit` event, so SPA logins that `preventDefault` + fetch don't trigger it yet. | all |
| 62 | ◐ | **E2E-encrypted sync** (done): **`flux://sync`** — bookmarks + sessions + **browsing history** across devices, account-optional + local-first via a **bring-your-own synced folder** (Dropbox/Syncthing/iCloud/USB); no Flux server. `sync.rs` writes one AES-256-GCM blob (key = Argon2id(passphrase, salt-in-blob)); the sync service only sees ciphertext. Merge unions items (bookmarks by url+folder, sessions by name, history by url keeping the higher visit count) **and now propagates deletions via tombstones** (`tombstone.rs`: per-store deletion ledger synced in the blob, newest-wins; a re-add beats an older tombstone). History is capped (top ~4000 by frecency) so the blob stays small. **Auto-sync timer** (opt-in toggle): re-syncs every ~3 min while unlocked + once on unlock/toggle-on, emitting `flux://sync-done`/`-error` so open pages refresh. Persisted format migrated to an `{items,tombstones}` envelope (tolerant of the legacy bare-array). 13 crypto/merge/tombstone tests. _Remaining:_ **open tabs** ("tabs from your other devices" — a read-only per-device view, not a merge; deferred as its own UX); explicit **conflict UI** (today: union + last-writer-wins on edits, tombstones for deletes). **cr-sqlite CRDT path — prototype landed** (`crsync` feature, `crsync.rs`): SQLite CRR tables + `crsql_changes` changeset exchange converge conflict-free (3 tests; removes the history cap + hand-rolled tombstone ledger). Next: wire to the live bookmark/session/history stores, encrypt changesets through `sync.rs`, ship the native cr-sqlite extension per platform. | under-served |
| 63 | ◐ | Built-in proxy / VPN / Tor window hook (bring-your-own provider). **Proxy** (done): Settings → Privacy → Proxy (HTTP/SOCKS5); `proxy.rs` ProxyState (persisted to `proxy.txt`, validated http/socks5 only) applied via Tauri's `WebviewBuilder::proxy_url` on every tab + panel webview (opt-in — a bad/empty value degrades to direct, never breaks browsing). `proxy_get`/`proxy_set` commands; unit-tested. _Remaining:_ per-private-window proxy (a dedicated proxied window/profile), a one-click **Tor window** preset, and a "reload all tabs" affordance on change (today it applies to new/reloaded tabs). | Brave, Opera |

## Epic: Extensions (Flux mini-extension API)

Decision (2026-06-15): Chrome/WebExtensions can't run in native webviews, so Flux
ships its **own** curated, permissioned extension model rather than chasing
WebExtensions compat or a raw userscript runtime. It reuses Flux's existing
JS-injection substrate (capture.js + the agent's injection compiler). The
architecture + security model are decided in **ADR 0008** (#96 ✅); #92–95 build
it out, starting with the manifest + loader (#92).

| # | P | Item |
|---|---|---|
| 96 | ✅ | **ADR + security model** — written: ADR 0008. Manifest-declared, capability-gated model; the powerful `flux.*` API lives in a **Rust broker** (content scripts are untrusted vs the page); a document-start **capability-token handshake** authenticates the extension (+ WebKitGTK script worlds where available, since WebView2 lacks isolated worlds); deny-by-default permissions w/ install consent; hard boundaries (no other-extension storage, no raw IPC, no blanket net/fs). |
| 92 | ✅ | **Manifest + loader** (done): `flux.extension.json` (id/name/version, deny-by-default `permissions`, `content_scripts` = match globs + js/css + run_at, optional `background`, `ui` contributions). `Manifest::parse` validates (id shape, known permissions, non-empty matches); `ExtRegistry` loads a folder, checks content-script files exist, and persists `extensions/registry.json` (install/list/enable/remove). Commands `ext_install/_list/_set_enabled/_remove` + ipc bindings. Shipped reference example `examples/extensions/hello`. (Folder-only for now; zip import + the install *dialog* come with the manager UI #95.) |
| 93 | ✅ | **Content-script injection** (done): on each page load `ExtRegistry::injection_for(url, phase)` assembles the CSS + JS of every *enabled* extension whose `@match` patterns hit, honoring `run_at` (document_start vs end/idle); injected via the existing `on_page_load`/`eval` path. Each extension's JS runs in its own **IIFE scope guard** (WebView2 has no isolated worlds — ADR 0008) carrying a frozen `flux` identity object (id/version/permissions). Match-pattern + glob engine is unit-tested. (The *callable* `flux.*` broker bridge — postMessage → Rust broker, grant-checked — lands with the API surface in #94, which replaces the identity shim.) |
| 94 | ✅ | **Permissioned API surface** (core done): a privileged Rust **broker** (`broker.rs`) backs `flux.runtime` (id/version/permissions), `flux.storage` (per-extension persisted KV), `flux.tabs` (query/open/navigate), `flux.dom` (read cached snapshot / inject JS). Each content script gets a JS shim that forwards calls to `plugin:fluxtab\|ext_broker_call` tagged with a per-extension **capability token**; the broker resolves the token → extension and checks every call against the manifest grants — **deny-by-default** (unknown calls + ungranted perms rejected). Grant model + tokens + storage + shim are unit-tested. (Deferred: `flux.ui` — side panel/toolbar/context-menu — lands with the manager UI #95; `flux.events` push subscriptions are a small follow-up under #95.) |
| 95 | ✅ | **Extension manager UI** (done): the footer 🧩 panel now lists installed extensions with name/version + permission chips, an enable/disable toggle, remove, and **install from a folder** (path input → `ext_install`, surfaces validation errors). Backed by the #92 registry commands. Reference example shipped at `examples/extensions/hello` (exercises `flux.storage`). _Remaining follow-up:_ `flux.ui` (extension-contributed toolbar button / side panel) + `flux.events` push subscriptions, and a native folder-picker dialog (currently a path input). |

## Epic: Under-served — Flux's wedge

Highly requested, poorly served by incumbents. This is where Flux differentiates
beyond "another Chromium skin."

| # | P | Item | The gap |
|---|---|---|---|
| 65 | ◐ | **DOM-aware integrated terminal**: the read-the-active-page half is done (#4) — `flux extract-json` / `flux dom` / `flux links` pipe the live page into the shell via the `$FLUX_RPC_DIR/active.json` bridge. **Agent driving the terminal** (done): an approved *"run &lt;cmd&gt;"* (or NL-proposed command) is **typed into the live PTY** — the user's real terminal session (cwd/env/history), visible, opening one first if none is up — and the agent **reads the new scrollback back** after it goes quiet (PTY has no exit signal → quiescence heuristic) to report/re-plan; the rm/destructive denylist is enforced via `shell_guard` before the keystrokes hit the PTY (headless `run_shell` stays as a fallback when no terminal can be brought up). Closes edit → run → read → fix. _Remaining:_ `cd $FLUX_TAB_DIR` into per-page artifact dirs; a clean exit-status signal (vs. quiescence). Nobody ships this. | core |
| 66 | ✅ | **Semantic everything-search** (done): ⌘K now ranks one list across open tabs (by title **+ captured page content**), bookmarks, and history via the local embedder (`omni_search`); large corpora are lexically pre-filtered then embedding-reranked so it's cheap per keystroke. Empty query = browse open tabs. _Follow-up:_ true synonymy needs the stronger embedder (#11); index history page text for offline content search (#69). | weak everywhere |
| 97 | ✅ | **`flux://omni` follow-up** (done): a compact Omni-index glance widget on the start page (live pages / vectors / embedder via `omniStats`, link into the full dashboard); toggleable/reorderable like the other widgets. | search |
| 67 | ◐ | **Scriptable macros** (done): footer **⏺** popover — record a flow (navigations + clicks + typing) into a named macro, then replay it. `macros.rs` (Macro/Step model, record buffer, async replay engine) + `macro-record.js` (selector-generating recorder, gated by `__FLUX_MACRO_REC__`) + nav capture from `dom_publish`. Passwords never recorded; persisted. _Remaining:_ **agent-authored** macros (NL → steps), **scheduling**, conditional/wait-for-element steps, and selector robustness (record/replay is best-effort on changed pages). | under-served |
| 69 | ◐ | **Offline archive + semantic search** (done): "Save page for offline" (⌘K / 📚) stores the page text locally (`archive.rs`); **`flux://archive`** semantically searches saved pages and reads them **fully offline** via the local `flux-embed` embedder (no network/service, unlike Omni #66; embeddings recomputed in memory, not persisted). Re-saving a URL updates in place; private pages skipped. _Remaining:_ full *rendered*-page (MHTML/SingleFile) capture for fidelity; fold the archive into Omni search as a source. | weak everywhere |
| 70 | ◐ | **Per-tab resource governor** (partial): `flux://resources` (📊, + ⌘K) shows overall process/free RAM and a per-tab payload (captured-DOM weight) + live/sleeping list, with one-click **Sleep background tabs** to reclaim RAM. _Honest gap:_ true per-tab CPU/RAM/network isn't shown — browser engines share processes across tabs, so it isn't cleanly attributable without deep renderer-process mapping; hard per-tab caps likewise need process control. Weight + sleep is the actionable part today. **On-disk storage** (done): the profile's size by group next to the RAM figures, with a banner when a group has grown past any sane size — a service-worker cache had reached 753 MB with nothing surfacing it, and there was no way to clear browsing data from inside Flux at all. (It was built while chasing a renderer crash and believed to be the cause; it wasn't — see #98/webview.rs — so this is hygiene, not a fix.) Clearing lives in Settings → Browsing data and is applied at the next launch, since the engine holds the files open. | requested, absent |
| 71 | ✓ | **Start page** — shipped: search hero, clock + real weather, recent, **editable** speed dial (persisted), quick actions, flowing wave, **feed headlines (#72), top sites (from history), scratchpad (persisted), calendar + world clocks**, and **⤢ expand on every widget** (recent / shortcuts / top sites / headlines / scratchpad / tasks / quick actions / calendar) → a full-content modal so the dashboard cards keep a fixed footprint. **Customize widgets** (done): a ⚙ popover toggles each widget on/off (`flux.start.hidden`, applied via per-card `display`) and reorders them with ↑/↓ (`flux.start.order`, applied via CSS `order` on the card grid; clock/hero pinned first via 1-based orders). Chose ↑/↓ over drag-and-drop — DnD over cards full of inputs/buttons is fragile. **Custom background** (done): the ⚙ popover takes an image URL or CSS color/gradient (`flux.start.bg`, applied to `.start`; empty = liquid backdrop). **Daily briefing** (done): a widget where the local agent (Gemma) condenses today's headlines into a few bullets, generated on demand and cached per day (`flux.start.briefing`), fully on-device. | locked-down elsewhere |
| 77 | ◐ | **Particle-flow home backdrop** (done): ~8k fine **monochrome** particles on a curl-noise flow leaving **fading trails** (frame-feedback) that bunch into flowing fingerprint/topographic ridges, over a velvet base with soft glow blobs — smoke/ferrofluid look (`LiquidBackground.tsx`, WebGL2 ping-pong FBOs: fade → additive splat → composite; no library, in the lazy start chunk). Gated hard for Flux's low-RAM wedge: only runs while the start page is the **active + focused + visible** tab (leaving it tears down the GL context), pauses on blur / hidden / `body.resizing`, field at ~0.8× res + ~40 fps, honors `prefers-reduced-motion`, and **auto-falls back to the wave** if WebGL2/a shader fails (never blank). Settings → Appearance toggle (default on). _Remaining:_ interactive/pointer-reactive; perf-measure on the user's GPU; tune density/trail length to taste. |
| 78 | ◐ | **Settings page** (done): **`flux://settings`** (⌘K "Open Settings", "⚙ Open full Settings ↗" from the footer popover) — a real full-width page consolidating the toggles that were scattered across the footer ⚙ popover and the Shields popover into organized sections: **Appearance** (website dark mode, bookmark bar), **Search** (default engine, suggestions, AI answers, Omni-answer-on-search), **Privacy & security** (shields + blocked count, HTTPS-only, tracking-prevention level, global block camera/mic/geo, clear-all-cookies, link to per-site permissions), **Navigation** (vim hints, gestures), **Memory** (hibernation on/off + timeout, memory-pressure eviction, live RAM readout + links to Resources/Tasks), and **Data** (links to Sync/Sessions/History/Bookmarks/Archive). State reads/writes the same store signals + flux-core commands as before — no new persistence. The Privacy section also lists **editable per-site exceptions** (shields-off / HTTP-allowed / lean-mode hosts, each with one-click "clear" to restore the default). The bookmarks UI (#22) shipped already. _Remaining:_ a real **appearance/theme** picker (accent + light/dark chrome — Flux is fixed velvet/glass today); the **extensions/equivalents view** is its own #24. |
| 107 | ✅ | **Built-in task manager** (complements #70). `taskmgr.rs` + **`flux://tasks`** (⌘K "Open Task manager"): a system-wide process monitor (sortable name / CPU% / resident MiB; `tasks_kill` ends a process), marking **Flux's own process tree** by walking parent pids to our own (accurate, not name-matched) and confirming before ending the main process. Real per-*process* CPU/RAM, which #70's per-tab weight can't give (shared engine processes). _Follow-up:_ a Shift+Esc shortcut. | Chrome task mgr |
| 108 | ✅ | **Network speed test** (Ookla-style). `netspeed.rs` + **`flux://speedtest`** (⌘K "Network speed test"): download / upload / latency + jitter against Cloudflare's public speedtest backend (`speed.cloudflare.com`, no API key); live phase progress over `flux://netspeed-progress`; throughput math unit-tested. _Follow-up:_ a history of past runs. | under-served |
| 137 | ◐ | **Scribe — handwritten per-course notebooks** (ADR 0014; foundation done) — GoodNotes-style paged notebooks (`flux://scribe`) on the whiteboard's ink engine (extracted to a shared `InkCanvas`), disk-backed in Rust (`scribe.rs`, opaque strokes), with one-way **publish a page to Onyx** as Markdown + embedded PNG into `<vault>/<course>/` (KB-indexed). **Document editor** (second pass): the page is a real rich-text editor (caret, selection, headings, lists) with ink inserted as draggable/resizable image objects from a Draw pane; **Tab/Shift+Tab** indent (nesting inside lists, 40px stops elsewhere); **✓ Proofread** — Gemma checks spelling/grammar locally (`scribe_proofread`) and every suggestion is validated in Rust against the text that was sent, so the panel can never offer a fix the page doesn't contain. Notebooks auto-index into the KB (change counter + settle rule), so "My notes" sees them without a manual reindex — **typed text only**, since ink is a PNG. **Handwriting → LaTeX** (done): 🔍 Transcribe reads a page's ink with the local vision model and indexes the result under its own `scribe-ocr` source, marked machine-read in every citation. _Next:_ **figure description inside research PDFs** (plots and diagrams are opaque even when the text layer is fine), and OCR for genuinely scanned PDFs — auto-fill the publish body so math pages become searchable/answerable (the `body` field is its drop-in target); then **restoring font size + the symbol palette to the document toolbar** (both live in the ink engine, which the rewrite demoted to the Draw pane), native stylus pressure/tilt (Windows build), more paper templates (Cornell/staff), lasso select-move, and page reorder. | GoodNotes ∩ private math notes |

## Epic: Files explorer

The Files tab shipped (ADR 0006): virtualized list, quick-access rail,
breadcrumb nav, sort/filter, open-with-default, **file operations**
(new/rename/copy/cut/paste/drag-move/delete-to-trash, multi-select, context
menu, confirm-on-destructive), **live directory watch**, **undo** for reversible
ops, and **marquee selection** (BACKLOG #83/#84/#85/#89, done). These take it
further:

| # | P | Item |
|---|---|---|
| 90 | P2 | **OS-native drag-out** (drag a file into Explorer/mail/an editor). Attempted via `tauri-plugin-drag` then removed: on Windows the gesture fired and the OS returned `DRAGDROP_S_DROP`, but the file didn't land — the transfer is inside the crate's native OLE code, unreproducible from the Linux dev box. Revisit with a Windows debug loop; may need a custom CF_HDROP data object. In-app drag + cut/paste cover moving files meanwhile. |
| 86 | ✓ | **Stream pathological dirs** (done) — `fs_list_stream` ships the listing in 2k-entry chunks over a Channel (head → entries… → done) instead of one giant JSON payload; the dir is read once, navigations supersede in-flight streams via a generation token, status bar shows "loading more…". Resolves the ADR 0006 single-payload tradeoff. |
| 87 | ✓ | **Flux cross-links** (done): **"Open terminal here"**, a **preview pane** (◰ toggle — image thumbnail / text contents / "Open in default app" fallback, reusing `attachment_read`), **"Open in browser"** (`file://` tab, PDFs via the built-in viewer), and **agent file actions** — **Summarize with Gemma** (local-agent summary in a modal) and **Rename by content…** (agent-proposed kebab-case name, confirm before applying). |
| 88 | ✅ | **Search within tree** (done): the ⌕ toggle flips the explorer's Filter box to recursive subtree filename search (`fs_search` — bounded BFS walk, skips hidden + heavy dirs, hit/depth/visit caps; unit-tested). **Fuzzy-ranked** (`fuzzy_score` — fzf-style subsequence with start/word-boundary/contiguous bonuses + shorter-better). Optional **✦ semantic** re-rank by filename relevance via the model embedder (`embed_remote_batch` — one batched `/api/embed`; `embed_batch`/`cosine`/`semantic_rerank`; no-op on the hashing embedder). _Note:_ semantic only re-orders the fuzzy (subsequence) matches — a query finding a *differently-named* file would need a pre-built file-embedding index (a bigger #11 follow-up); filename signal is also short. |

## Epic: Home widgets

| # | P | Item |
|---|---|---|
| 134 | ◐ | **Timer / stopwatch / alarms** — **shipped** as a **Time** start-page widget (stopwatch with laps; timer with presets/custom/pause; daily alarms), state in `clocks.ts` so it survives leaving the start page; an always-on driver rings via Web-Audio beep + a docked in-app banner + OS notification (`os_notify`). Alarms + timer length persisted. _Remaining:_ **multiple concurrent timers**; **one-shot vs repeat-days** alarms (today alarms are daily); label/sound per alarm; a world-clock consolidation; make the timer/alarm survive an app **restart** (persist running target, not just config). |

## Epic: Playground (arcade)

The offline arcade shipped (🎮 popout, `apps/shell/src/playground/`): a neon hub +
twenty-three self-contained `<canvas>` games — the arcade classics plus
Pac-Man, Dino Run, Stack, Frogger, Whack-a-Mole, Doodle Jump, Simon, Columns,
Missile Command, Bubble Shooter, Bejeweled, Centipede, and vs-AI Connect Four +
Reversi on a shared engine harness, local
high scores. These extend it:

| # | P | Item |
|---|---|---|
| 133 | ◐ | **Playground** — offline arcade **shipped** (23 games, local high scores, neon hub). _Remaining:_ **online play + leaderboard rankings** (a backend — likely a small Flux service or a BYO endpoint — with per-game score submission + a friends/global board; keep offline-first, opt-in, no account required to play); per-game **pause**; gamepad input; a mobile/touch control scheme; more games as the mood strikes. Vs-AI board games (Connect Four, Reversi) score as a win streak. |

## Epic: Themes, sync & polish (2026-08)

Shipped this cycle; the open half is listed per item.

| # | P | Item |
|---|---|---|
| 139 | ✅ | **Colour themes** (ADR 0015). ~300 hardcoded accent literals in `theme.css` collapsed into six **role-named RGB channels** (`--accent-rgb` is "the primary interactive colour", teal in Velvet and rose in Ember); a theme is now five base tones + six channels, ~20 lines. `palette.ts` reads the *computed* properties so the WebGL shaders (Liquid, Aurora), canvases (Trail, Omni) and the terminal follow the theme without duplicating it in TS. Status colours are separate channels and the terminal's ANSI red/green/yellow stay fixed — those are what programs mean by them. Applied before first paint. Ships **Velvet** + **Ember** (oxblood/rose). _Open:_ a light theme (the glass + shadow treatment assumes a dark floor, so it's more than a channel set); new chrome must be checked against both themes since a hardcoded literal fails silently. |
| 140 | ✅ | **Scrollbars, once** (`::-webkit-scrollbar` globally against theme tokens). Fixed a live cross-platform bug: since Chromium 121 a non-`auto` `scrollbar-width` discards every `::-webkit-scrollbar` rule, and four bars declared `thin` *and* a hidden bar — so Windows drew scrollbars the design had removed while Linux hid them. Guarded by `theme.test.ts`. _Unverified:_ appearance on the real WebView2/WebKitGTK builds — headless Chromium uses overlay scrollbars with zero layout width, so a harness proves nothing. |
| 141 | ✅ | **Agent write access to notes** (ADR 0016). `/note` or plain language → a proposal card showing the exact text → approve → written. Closed additive-only vocabulary (no variant can replace/delete), plan and apply as two unjoined commands, vault-relative paths canonicalized and required to exist. _Open:_ `plan_note`'s prompt is unverified against real Gemma output — the schema constrains shape, not judgment. |
| 142 | ✅ | **Sync: tasks, calendars, sessions-with-workspaces** (ADR 0017). Tasks gained `updated_ms` (without it, ticking a box never propagated); calendar feeds + local events sync; sessions restore into their workspaces **by name**; idle auto-sync no longer rewrites an unchanged blob; a forked sync identity is reported instead of silent. _Open:_ the key is never persisted, so sync is idle after every restart until the passphrase is typed — an opt-in keychain option (as the vault already has) would remove the daily friction. |
| 143 | ✅ | **Scribe syncs through a shared folder.** Per-page merge (union by id, newer `ts` wins), page tombstones inside the notebook file, a folder watcher so a notebook from another device appears without restarting. Plaintext, so block-level dedup works. _Open:_ notebook-level conflicts where both devices edit the *same page* still resolve last-writer-wins on that page. |
| 144 | ✅ | **LaTeX blocks in Scribe** (`Σ`, `Ctrl+M`, or typing `$$x^2$$`). Source lives in `data-tex`, rendering is throwaway, equations index as their LaTeX. Gemma writes the same node shape. KaTeX is a 77 KB lazy chunk. |
| 145 | ✅ | **Search spotlight** (`Ctrl+Shift+K`) and the **collapsed sidebar rail** (flux-plan1) — pinned tabs, open tabs, workspaces, footer. |
| 146 | ◐ | **Chrome-JS budget creep.** 63.8 → 66.1 KB gzip across this cycle (budget 70). `ask.tsx` and `palette.ts` are the obvious lazy-import candidates — neither is needed at boot. Worth doing before the next feature rather than at 69 KB. |
| 147 | ◐ | **Hand-mirrored lists across the FFI boundary** keep drifting: `OWN_SOURCES` (Rust vs TS) and `shortcuts.js` vs `shortcuts.ts` both broke silently this cycle. The shortcuts pair now has a parity test; **`OWN_SOURCES` still has none**. Specta (#12) covers types, not constant lists — a small test per pair is the cheap fix. |
| 148 | ◐ | **Session tab provenance.** Closing "a session's tabs" matches by URL, because nothing records that a tab came from a session. Real provenance would have to survive every move, pin and workspace change — probably not worth it, but the current behaviour closes a copy you opened yourself. |

## Epic: Chrome layout & the PDF viewer (2026-08)

The layout half has one thesis: on a 16:9 window **height is the scarce axis and
width is not**, and Flux was spending height on things that don't need it. The
viewer half is four reported complaints and the two latent layout bugs that
measuring them turned up.

| # | P | Item |
|---|---|---|
| 149 | ✅ | **Launcher column.** The Flux-pages strip and the terminal-apps strip were horizontal bars docked above the content card, costing ~76px of page height on every tab for a launcher used a few times an hour. Both are now vertical, in a fixed 154px column immediately left of the calendar/mail column, and the card claims the height back. Fixed width on purpose — the contents are chips at one font size, so a splitter would only be a way to make it wrong. Shed after the web panel and before the dock under `responsive()`, since it's the cheapest column to keep. Same `flux.pagesbar` toggle and localStorage key as before, so nobody's "off" silently became "on". |
| 150 | ✅ | **Sidebar toolbar + footer fold.** Both hold a dozen controls you use occasionally and read past constantly; folding gives the height to the tab rail, which is the reason the sidebar is open at all. Persisted, and the same flag applies in the **collapsed icon rail** — where the footer previously redrew as a two-column icon grid, i.e. exactly the height collapsing was meant to reclaim. The sidebar toggle and the fold caret always stay visible: folding must never be a way to lose the control that unfolds. Fixed a latent bug found while measuring — the toolbar's eight buttons never fit 252px and the sidebar hides its overflow, so the last two were being clipped off the right edge; the row wraps now. |
| 151 | ✅ | **Workspaces move to the footer.** The right-edge dot rail said nothing about which workspace was which until you hovered one, and charged a 22px gutter down the sidebar's entire height for the privilege. One footer button (carrying the active workspace's colour) opens a portaled panel with every control the rail had — switch, recolour, rename, delete, new. It renders in both sidebar states, so the collapsed rail's separate workspace button is gone too. Anchored by measuring the footer and sidebar rather than assuming either. |
| 152 | ✅ | **System monitor in the connections rail.** CPU, memory, GPU, VRAM, disks and network as one dense card above the Trail. A second *view* of `tasks_stats`/`gpu_stats`/`tasks_disks`, not a second collector. Polling stops when collapsed and while the window is hidden; disks poll on a 30s timer of their own because enumerating volumes measured 30–53s on a machine with a sleeping external drive. Absent hardware is omitted rather than zeroed — a GPU row reading 0% claims the card is idle rather than unread. _Open:_ no history sparklines (the task-manager page has those, and this is a glance surface); swap shows only when non-zero. |
| 153 | ✅ | **Preview harness repaired.** `npm run preview:ui` had been failing to build since App.tsx started importing `addPluginListener`, and even once built it came up empty because the mock had no `shell_snapshot` — so `refreshTabs` threw on `.tabs` and store init aborted, leaving no tabs, groups or workspaces. Both fixed, plus mock `tasks_stats`/`gpu_stats`/`tasks_disks`. This is the only way to actually *look* at chrome changes without a display ([[diagnose-invisible-failures-first]]); all four items above were verified in it, and it caught the clipped toolbar. |
| 154 | ✅ | **Launcher column is icon-only** (154px → 54px). Names return on hover and on keyboard focus through `RailTip` — one portaled `position: fixed` label for the whole column, because the bars scroll and `overflow-y: auto` clips on both axes, and because the native `title` tooltip is a second late and mispositioned on WebKitGTK. |
| 155 | ✅ | **PDF viewer: position, navigation, notes, zoom.** Four separate complaints, one cycle. _(a)_ Reading position, zoom, bookmarks and comments persist per document (`pdfstate.ts`, 8 tests over the untrusted-storage parser) — the viewer unmounts on tab switch, so it used to reopen on page 1. _(b)_ Page jump `⟨ n / N ⟩` in the toolbar. _(c)_ Bookmarks + comments as a **sidecar** Notes panel, deliberately not burned into the file the way Edit-mode markup is. _(d)_ `Ctrl`+wheel / `Ctrl`+`=` were hitting engine zoom and scaling the whole chrome; the viewer claims and `preventDefault`s them, and zoom re-anchors on the current page. _Open:_ comments are page-level, not anchored to a point on the page — anchoring would need a click target and a coordinate space the annotation layer already has, so it's a natural follow-up. Notes are keyed to the file path, so moving the file loses them; a content hash would survive a move but not an edit. |
| 156 | ✅ | **The PDF viewer never fitted its card.** `.card` uses `place-items: center`, and a centred grid item is sized to its content rather than its track — so any page zoomed past the card width pushed the toolbar off the left edge, which is where the zoom controls live. Pre-existing and independent of the PDF work; found by measuring the layout in the preview harness. Worth auditing the other full-bleed internal pages for the same assumption. |
| 157 | ✅ | **Hiding a scrollbar hid the only way to scroll.** #140 resolved a genuine Windows/Linux split by hiding the bars on the chip strips; the bookmark bar and the tab folders are horizontal-only scrollers, so that left them with nothing to drag and a vertical wheel the engine needn't translate — everything past the first screenful was unreachable. Bars restored (global treatment, never re-styled per component — `theme.test.ts` enforces that and caught an attempt to), plus `hscroll.ts` translating a vertical wheel. The system monitor was my own variant of the same error: a 34%-capped inner scroll region nested in a column that already had two. It sizes to its content now and the column scrolls instead. New rule recorded in `theme.css`: a scrollbar may only be hidden where the wheel already works *and* the gutter is a large fraction of the element (two narrow vertical rails qualify; nothing horizontal does). _Open:_ not visually verified — the preview harness's browser backend died mid-session, so this one rests on the static checks and the reasoning. Worth a look on the next Windows build. |

## Epic: Agent — act, don't narrate (2026-08)

| # | P | Item |
|---|---|---|
| 158 | ✅ | **Plain-language requests reach the agent loop.** The tools existed but only behind `/task` / `/fix`; plain chat had none and the model wasn't told, so "go through the PDFs in /x and summarise them into Onyx" produced a plan, an offer to start, and the same offer again. `agentintent.ts` (`looksAgentic`, 8 tests) routes work requests to the adaptive loop — checked *before* note detection, because a request naming both files and a notebook has to read the files before any note can be drafted. Safe to be generous: every side-effecting step still stops at its approval card. Also closed the capability gaps that would have stalled it anyway — `list <dir>` (no approval; listing isn't a side effect), `read <path>` extracting real text from PDFs via the viewer's own PDF.js path (page-numbered; a scan says so), and a `note` step so a task can finish in the user's notes. Step cap 10 → 28, and `note_plan`'s context argument — which nothing had ever passed — now carries the files that were read. _Open:_ **not verified against real Gemma output.** The routing, tools and prompts are tested; whether the model reliably emits `list`/`note` in that vocabulary is a question only a live run answers. |
| 159 | ✅ | **The loop's history is lossy by design.** Each step's result is capped at 1500 chars, so a re-plan sees only the head of a long read. That's right for deciding the *next* step and wrong for anything that needs the whole document — which is why the note draft reads from `ctxFiles` instead. Two paths carrying context is a seam worth collapsing; a summarise-as-you-go step (one note append per file) would also bound it naturally and would beat the current all-at-the-end draft on a folder bigger than the context window. |
| 160 | ✅ | **One WSL path bridge, not one per command.** On Windows a Unix path lives in WSL; `read_text_file` and `write_text_file` each had their own `wsl.exe` shell-out for it. The agent's new `list <dir>` and PDF reading were call sites three and four, and neither knew — so `/home/…` gave "The system cannot find the path specified. (os error 3)". Bridge extracted to `files.rs`, all four entry points through it: listings via `find -printf` (not parsed `ls`), bytes via base64 in transit because raw binary through `wsl.exe` isn't guaranteed intact. The base64 decoder is deliberately not `cfg(windows)` so the test suite actually runs it (4 tests over padding, whitespace, all 256 byte values and a PDF header). Windows-only code verified by extracting the bridge into a standalone crate and `cargo check --target x86_64-pc-windows-msvc` — the full crate can't cross-check, a C dep needs MSVC's `lib.exe`. _Open:_ compiled, not executed. `wsl.exe` interop behaviour (distro selection when several are installed, `bash -lc` startup cost per call) is unverified on a real machine. |
| 161 | ✅ | **Never splice a path into a shell script.** The bridge listed Flux's own repo instead of the requested folder, with exit 0. `Command` quotes args by MSVCRT rules and `wsl.exe` re-parses by its own, so the escaped inner quotes didn't survive and `d=""`; then `cd -- "$d" && find .` — where `cd ""` succeeds and stays put — returned the inherited working directory. Paths are arguments now (`bash -c script name "$1"`), `find` names the directory rather than trusting the cwd, empty exits 2, and the resolved path is sentinel-marked so a shell banner can't be read as it. Verified by extracting the real scripts from the source and executing them: correct for the reported path, `~`, spaces and an embedded double quote; exit 2/3/4 for empty/missing/not-a-directory; a binary PDF round-trips byte-exact. Same fix on the write path, where the failure mode was writing an approved edit to the wrong place. _Lesson worth keeping: a wrong answer with a zero exit status is worse than an error, so every check exits distinctly._ |
| 162 | ✅ | **The adaptive loop can no longer repeat itself to death.** One goal spent 23 steps re-issuing the same `list`. The planner prompt already said not to; a prompt isn't a guarantee. Repeats are refused and fed back as a fact ("SKIPPED — you already ran this"), and three strikes ends the loop with what was achieved intact. |
| 163 | ✅ | **A tool's output shape decides the next command.** The agent couldn't read the PDFs it had just listed: `list` returned bare filenames, so the model's next step was `read 01-lecture.pdf` — a relative path resolved against Flux's launch directory. `list` returns full paths now, and a bare name also resolves against the last directory listed (`agentpaths.ts`, 7 tests); with nothing listed yet the name is left alone, because guessing a directory turns "not found" into "read the wrong file". Failed reads name the path they tried. _The general lesson, worth applying to every tool added later: in a loop where the model writes each step from the previous result, the RESULT FORMAT is an interface — bare names invite bare names back._ |
| 164 | ✅ | **An absent snapshot is not a neutral state.** A PDF with no text layer published nothing — correct for the KB, wrong for the live snapshot, because "no page context" and "a page with nothing readable in it" look identical to the model. Asked about the open document it speculated about Flux's plumbing and asked the user to paste the slides in. The snapshot now states the case, as does the partial one (only N of M pages have text), which was previously logged and never surfaced. The viewer shows both, and offers OCR for a partly-image deck rather than only a fully scanned one. _Same lesson as #163 from the other direction: what a tool DOESN'T say is also an interface._ |
| 165 | ✅ | **Truncation is a retry, not homework.** A 26B model made structured calls fail with "raise num_predict with FLUX_OLLAMA_OPTIONS" — a correct diagnosis handed to the user for a request the machine could just repeat. Grammar-constrained generation stops when the object closes, so a bigger ceiling is free for any reply that would have fitted; a truncated one now retries at double the room up to 8192. The single global cap also stopped making sense once `plan_note` was generating the user's actual note (thousands of tokens) alongside phishing verdicts (hundreds) — doubling on demand keeps num_ctx, and the KV cache, small for the calls that don't need it. When the prompt itself fills the window the retry is skipped and the error says so, since "use a terser model" is then the wrong diagnosis. 3 tests; one caught a real bug in the first version of the headroom check, which compared two clamped values and would have aborted the retry on every short prompt. _Open:_ not exercised against a live 26B. |
| 166 | ✅ | **Named places.** "Save it to onyx under 00 - Optimization" needed an absolute path, twice over: the note planner saw existing note *files* but never the vault's folders (so `new_note.folder` came back null or invented), and the file tools accepted paths only (so `list onyx` meant nothing). `places.rs` resolves `onyx`/`scribe`/`downloads`/`home` plus the vault's top-level folder names, and one definition feeds all three consumers — note-planner prompt, agent system prompt, and `resolveAgentPath` — so they can't disagree about the same vault. Matched case-insensitively on a whole leading segment only (`onyxdata/` is not the vault); resolved per call, not cached, because the vault path is a setting and a stale one files notes in the wrong directory; only existing directories are offered. 5 Rust + 4 TS tests, and mocked in the preview. _Open:_ folder matching is left to the model — it gets the names verbatim but nothing enforces that `new_note.folder` is one of them, so a near-miss still creates a new folder. A server-side check against the listed names would close that._ |
| 167 | ✅ | **OCR is the agent's job, not the user's.** Reading a scan ended in "open the viewer and click Read with OCR" — handing back the task. The file tools now OCR automatically when there is no text layer, with per-page progress, flagged as machine-read wherever the text is used. Capped at 40 pages unprompted (a subprocess per page; a 300-page scan is five minutes of a loop that looks hung) — longer stays a deliberate act from the viewer. Missing-tesseract and nothing-legible are reported separately. Page loop shared with the viewer's button via `pdftext.ts` rather than copied. _Open:_ no way to interrupt the agent's OCR pass mid-document; the viewer's is equally uninterruptible, so both want the same cancel._ |
| 168 | ✅ | **A page that owns its snapshot must opt out of the generic one.** `InternalPage` publishes every Flux page's rendered DOM as the tab snapshot; for `flux://pdf` that is the toolbar (filename, page count, zoom) and it overwrote the document text `pdf_publish_text` had written. Harmless-looking until this cycle's page-jump readout made the DOM change on every scroll — the publisher is MutationObserver-driven, so the clobber went from a startup race to continuous. The viewer opts out; the snapshot now also carries the file's absolute path (previously a tooltip only, so uncopyable by user or agent), and the toolbar filename copies it on click. _Open:_ the opt-out is a URL check. Any future page that publishes its own snapshot has to remember to add itself — a registry, or having `dom_publish_internal` refuse to overwrite a richer snapshot, would make it structural._ |
| 169 | ✅ | **Unload the model from VRAM** — a button in the model picker. Keeping a 12B resident for 30 minutes buys latency between the user's turns and costs several GB while they're doing anything else; the only escapes were waiting it out or `ollama stop` in a terminal. Uses the HTTP API (`keep_alive: 0`, the same knob inverted) rather than the CLI, so it still works against a remote `FLUX_OLLAMA_URL` where the local binary would mean a different process. Reloads on the next message. _Open:_ no indication of whether a model is currently resident — the button is always offered, and pressing it when nothing is loaded is a harmless no-op that still reports success. `/api/ps` would let the picker show what's actually in VRAM._ |
| 170 | ✅ | **Bound what the model is asked to write.** A 26B summarising six lecture PDFs blew past 8192 output tokens (~32KB) and failed with nothing usable — the note prompt had no length guidance at all, and 24KB of sources in context invited it to write them back out. `maxLength` on every body field in the JSON Schema (grammar-constrained, so a hard stop where the backend honours it) plus explicit prompt guidance: 300-800 words, a short section per document, stop rather than repeat. The truncation error no longer leads with "raise num_predict", because at that size `num_ctx` cannot hold prompt and answer together and the suggested value wouldn't fit. _The general shape: an unbounded output field is an invitation, and the fix belongs in the schema, not in the token cap._ |
| 171 | ✅ | **Repair the model's JSON quoting slips.** "invalid escape at line 1 column 2411" was the note prompt getting what it asked for: it requests LaTeX, and `\int` / `\,` inside a JSON string are not valid escapes, so serde rejected the entire reply. Common wherever maths or Windows paths appear. A stray backslash inside a string literal is escaped to a literal one; `\u` only counts as an escape with four hex digits after it. Runs ONLY after a strict parse fails, so valid output is never rewritten, and an unrepairable reply reports the ORIGINAL error rather than one about text the model never produced. All 13 structured parse sites share one helper. Extended to the other two slips after a second report ("expected `,` or `}`"): an unescaped `"` inside prose, resolved by whether the next non-space char can legally follow a string, and raw control characters, which are never ambiguous. 8 tests incl. LaTeX surviving as literal backslashes, valid JSON round-tripping untouched, a genuine closing quote not being swallowed, and all three slips at once. _Open:_ ambiguous cases remain — `"he said "hi", then left"` still splits at the comma. Unfixable without a real grammar; the schema-constrained path should make it rare._ |
| 172 | ✅ | **Size the prompt to the window it shares with its answer.** "No room to grow" was two faults: the retry gave up when the DOUBLED cap didn't fit, leaving thousands of usable tokens unused (it clamps to what fits now, and only errors when nothing larger fits at all); and `plan_note` reserved a fixed 24KB of context regardless of the window, so a 10,150-token prompt met a 16,384-token ceiling. Context is now sized from `context_window()` with the bounded answer's space reserved first. The error ranks the fixes by what actually helps — ask for less, clear the file context, then raise `num_ctx` — instead of leading with the one that costs VRAM. _Open:_ the real answer for a folder of documents is still #159, one note append per file; every fix here just buys headroom within a single request._ |
| 173 | ✅ | **Summarise per document, not per folder** (closes the open half of #159). `summarise <path> into <where>` reads ONE file and drafts its note in a single step; the planner prompt and its worked example now teach that shape instead of read-everything-then-note. Bounds both the prompt and the answer by the largest single file rather than the folder — the root cause behind the truncation, the no-room-to-grow and the runaway-length failures — and makes progress durable: each summary is approved and written before the next file is read, so stopping halfway keeps what you had. Later documents append to the note the first created. `agentsteps.ts` parses the step (5 tests, incl. splitting on the LAST 'into' so a path containing the word survives); `planNote` takes explicit context so a summary sees one document, not everything read so far. _Open:_ whether the model reliably emits one step per file is unverified against a real 26B — the repeat guard (#162) bounds the damage if it doesn't._ |

## Epic: Research-driven optimization

Top priorities distilled from a 40-paper survey of web-application + compiler
optimization (synthesis in `research/RESEARCH.md`, index in
`research/curated.json`). Framing constraint: Flux embeds the OS webview engine
(V8 / JavaScriptCore) and **cannot patch its register allocator / JIT / GC**, so
the shippable wins are at the network, blocking, caching, agent, and Rust-core
layers — not inside the engine. Each item cites its paper(s) and the existing
BACKLOG item it extends.

Status: implemented + unit-tested for #99–#106 (and #138) and **wired into the UI**; the
only remaining pieces are engine-gated (noted per item).

| # | P | Item |
|---|---|---|
| 99 | ✅ | **Hot/cold filter tiering** (extends #57, arXiv 1810.09160). The tokenized engine is already fast, so the win is **not re-running it** — verdicts are memoized per `(url, source-host, type)` in a bounded TTL/LRU decision cache (`shields.rs`, cleared on rule rebuild), and firing rules are recorded into a **hot set**; the Shields popover shows cache hit-% + active-rule count (`shields_hot_rules` exposes the full list). _Follow-up:_ persist the hot set across sessions to seed a sync/async split. |
| 100 | ◐ | **HTTP/3 + QUIC** (network, arXiv 2102.12358, 2306.11643). _Done:_ `enable_http3()` makes the **WebView2** engine negotiate QUIC explicitly (`--enable-quic`, merged into `WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS`; `FLUX_WEBVIEW2_ARGS` to add more); no-op on WebKitGTK (limited H3). _Remaining:_ DoQ-over-DoH + connection coalescing + 0-RTT are governed by the engine's network stack and aren't directly configurable through Tauri — revisit if/when a custom resolver lands. |
| 101 | ✅ | **TTL/LRU cache utility** (core, arXiv 2602.06074). Done: bounded `TtlCache<K,V>` (`cache.rs`, LRU + per-entry TTL, hit/miss stats, 7 tests), in use by shields (#99) and available for favicon/metadata/settings/LLM-response caching. _Follow-up:_ adopt it in those other call sites + on-disk persistence for durable entries. |
| 102 | ◐ | **Agent latency levers** (extends #82, arXiv 2203.16487). _Done:_ the model is kept warm (`keep_alive`, default 30 m → no per-call reload), context is capped (`num_ctx`), and an options passthrough (`FLUX_OLLAMA_OPTIONS`, merged over defaults) is the hook for speculative-decoding/draft-model knobs when the local Ollama exposes them. _Remaining:_ true draft-model speculative decoding + INT8/INT4 quantization depend on Ollama-side support; document a recommended setup. |
| 103 | ✅ | **Confidence-gated predictive prefetch** (ties #70, #45, arXiv 1906.00877). `prefetch.rs` — per-origin Markov chain (LFU decay, confidence + min-sample thresholds). The chrome records each navigation transition on page-load-finished and **preconnects** (`webview_preconnect` → `<link rel=preconnect>`) to the predicted next hosts; the pressure gate is driven by the resource governor's memory poll (#70). _Follow-up:_ persist the model across sessions; scale prefetch depth by hit-rate. |
| 104 | ◐ | **Agent safety guards** (extends #8, #82, arXiv 2511.19477). _Done:_ a **destructive-action guard** enforced in the execution layer — every agent click's compiled JS reads the element's *real* accessible name and aborts on a Rust-defined deny-list (delete/pay/place-order/refund…), never trusting the model (`flux-agent`: `is_destructive` + `DESTRUCTIVE_TERMS` + the injected guard); destructive intent is flagged in the activity feed. _Remaining:_ accessibility-tree primary context + versioned element refs (`snapshot_ver:elem_ref`) + batched actions — the larger agent-architecture work. |
| 105 | ◐ | **Per-site lean mode** (perf, arXiv 2106.08948, 2308.16729). _Done:_ `leanmode.rs` — opt-in per host via the **Shields popover** ("Lean mode here"); when on, a curated performance filter list (`assets/lean-filters.txt`: tag managers, analytics, A/B, session replay, chat/social widgets) blocks heavy third-party scripts on top of shields, wired into the request interceptor. _Remaining:_ the **dynamic per-function dead-JS elimination** (coverage trace → empty-body/lazy-load with a screenshot-diff oracle) needs a webview coverage hook the native engines don't expose through Tauri. |
| 106 | ✅ | **Smarter hibernation eviction** (ties #45, #70, arXiv 1202.5539). The memory-pressure path now calls `hibernate_rank` to sleep tabs **Belady-style** — least likely to be needed next, discounting idle time by the #103 prediction and keeping predicted-return tabs — with a plain-LRU fallback. _Follow-up:_ surface dark-mode/shields/hibernation as measured **battery** features (arXiv 2205.11399); rematerialization policy. |
| 138 | ◐ | **Use the CPU we have** (core). Flux was thoroughly *concurrent* (114 `spawn_blocking` sites, per-thread adblock engines, single-flight background refresh) but not *parallel* — every O(corpus) loop ran on one core. _Done:_ KB retrieval is a parallel scan into a bounded top-`k` (~8x on 16 cores, ranking provably identical to the serial scan incl. tie order); reindex chunks in parallel and batches its embedding requests instead of one HTTP round trip per document; an accidental O(n²) in `n_chunks` removed. Embeddings moved out of the JSON index into an int8 binary sidecar (one contiguous matrix, per-row scale): at 60k chunks 623 MB → 125 MB on disk, 1.09 s → 207 ms to hydrate, 176 MB → 44 MB resident, recall@8 vs exact f32 measured at ≥99%. _Remaining, in order of value:_ **(a) the JSON is now the bigger half** (81 MB of the 125 MB at 60k) — `KbChunk` stores `source`/`doc_id`/`title`/`path` as owned `String`s *per chunk*, so a ten-chunk note repeats its title and path ten times; intern them or key chunks to a doc index. **(b) incremental persist** — reindexing one source rewrites every source's vectors. **(c) an ANN index** — retrieval is still a full linear scan, and parallelism only buys a constant factor where HNSW/IVF would change the complexity class — but the numbers say a personal KB never gets there (~2M chunks to reach even 100 ms), so this stays parked. **(d) explicit SIMD for the int8 dot product** — currently a scalar fold left to autovectorization. |

## Decisions wanted (not yet scheduled)

- ✅ **Search backend** (#68, done): `flux-search` ships a template-based engine
  config + resolution; the user's own engine drops in via `search_add_engine` +
  `search_set_default`. Remaining: live suggestions UI (#32).
- Terminal multiplexer protocol compat (tmux control mode)? Revisit after #15.
- ✅ **Extension story** (decided 2026-06-15): Chrome/WebExtensions can't run in
  native webviews → Flux ships its **own curated mini-extension API** (new epic,
  #92–96), not WebExtensions compat or built-in-only. Start at the ADR (#96).
- CLI: replace the hand-rolled parser with `clap` once flags exceed ~6 (`cli.rs`).
- **Voice — better wake word + STT models.** The "Hey Gemma" MVP reuses Vosk
  partials for the wake word and a small Vosk model for transcription, which is
  serviceable but has false triggers and limited accuracy. Bring in (a) a
  dedicated wake-word model — **openWakeWord** (ONNX, custom "hey gemma" phrase) or
  **Porcupine** (Picovoice free tier) — so detection is robust without constantly
  transcribing, and (b) a more accurate STT — a **larger Vosk model** or
  **whisper.cpp** (`whisper-small`/`base`, still fully local) for the command
  utterance. Keep the privacy invariants ([[voice-privacy-invariants]]): all local,
  pre-wake audio discarded, nothing persisted.
- **Editor column follow-ups (#174).** The nvim column ships as one fixed pane on
  the right, which covers the "always have an editor open" case but leaves a few
  obvious gaps: (a) the boot command is hardcoded to `nvim` — it should be a
  setting, so `hx`/`kak`/`emacs -nw` work without a code change; (b) there's no
  way to open the file a page links to (or the current tab's URL) *in* that
  column, which is the natural next step now that the editor is always there;
  (c) the column can't move to the left of the page or split into two editors;
  (d) `persist="off"` is pinned, so the editor doesn't survive a Flux restart
  even when the broker is installed — reattaching would need the boot command to
  be skipped on an already-running session, which the pending-command mechanism
  can't currently express.
- **Cloud escalation follow-ups (#175, ADR 0018).** Shipped as one provider behind
  a session switch. Known gaps: (a) no token/cost accounting — an escalated
  session can run up a bill with nothing in the UI saying how much; (b) the
  escalation is all-or-nothing for the session, where the natural unit is often
  *one task* ("summarise this folder with Gemini, then go back"); (c) nothing
  redacts the prompt before it leaves — vault notes and terminal output go as-is,
  and a "strip secrets first" pass would make escalation far less costly to
  choose; (d) Gemini's safety filters can refuse a page the local model handles
  fine, and the failure is currently just reported, not retried locally — an
  automatic fall-back-to-local on a safety block would be strictly better;
  (e) `gemini_models()` is fetched per menu-open with no cache.
